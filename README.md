# iRAS

> 循环水养殖工程设计平台

[![License: AGPL v3](https://img.shields.io/badge/License-AGPL_v3-blue.svg)](https://www.gnu.org/licenses/agpl-3.0)
![Version](https://img.shields.io/badge/version-2.5-emerald.svg)
<!-- IRAS_V22_README_VERSION: 徽章自 v2.0.0 起积压两版未改, 随版本统一维护。
     v2.3.3 订正: 本 README 曾长期停留在 v2.2 副本 (真 v2.3 README 不可得,
     见下方 v2.3 章节头注), v2.3 内容由工程记录/manual/交接文档补齐重建。 -->
![Selftest](https://img.shields.io/badge/selftest-42%2F42-brightgreen.svg)
<!-- IRAS_V233_DOC_BATCH: 徽章随四/五批入列的 S1–S4 更新为 29/29。
     ⚠ 正文各批次小节里的 "28/28" "27/27" 是【该批次当时的门禁快照】,
     属历史陈述, 不随当前值改写。 -->
![Status](https://img.shields.io/badge/status-stable-brightgreen.svg)

iRAS 是一个面向**水产养殖工程师、设计院、高校研究者**的循环水养殖系统(Recirculating Aquaculture System)工程设计开源工具。覆盖**工艺设计 → 碳酸盐平衡水化学 → P&ID 工艺图 → 设备清单 → 投资财务评价 → 16 章可研报告生成**的完整工作流。

🌐 **在线使用**
- 主站:<https://iras.cn>
- 国内服务器:<http://iras.dengdengtech.cn/>(国内访问更快)

---

## 质量保证

每个版本发布前都要通过一整套自动化门禁:页面内自检、13 条数值基线逐位比对、
跨文档版本口径、文档默认值与代码的一致性、上下游数据契约、手册正文数字与代码符号的
比对,以及手册表格与案例章**直接从源码/真实模型生成**(值由代码出、释义由人给,不手抄)。

其中**页面内自检随产品发布**——在任意 iRAS 页面地址后加 `?selftest=1` 即可运行,
当前 **35 项**全绿(徽章与实测由门禁锁定,不一致即转红)。
其余门禁是开发期工具链,不在发布包内。

**每条门禁上线前都跑过负对照**——没跑过负对照的校验器与没有校验器等价
(本项目已多次遇到「断言写好但全绿」:判据宿主填错、锚定工况走不到、告警写了没人查)。

⚠ 三条反复验证过的经验:

- **审计器把被审对象跑瘸了,得到的"干净"是假的;把自己跑瘸了,得到的"红"也是假的。**
  下游审计两轮内此类问题出现 7 次(缺依赖、共享 storage、转义丢失、深度截断……),
  全部由负对照或假红暴露,读代码读不出来。出现"全绿"或"全红"这类整齐结果时,先怀疑量具。
- **判据要作用在"生效的东西"上**:注释里提到的 id 不是 id,历史叙述里的旧默认值不是当前值,
  运行时"读过"不等于"在用"。分不清这三者,校验器就会满屏假红或一片假绿。
- **判据错比代码错更贵**——它会让人去改对的东西。v2.4 有两次:一次把合规的设计判成红,
  差点逼出错误的"修复";一次是登记表式判据**漏登记**,于是手册版本号停在两个版本前
  而门禁全绿。**登记表式判据的强度完全取决于表的完整性。**

## 主要功能

> **v2.5（2026-08-18）⭐ 当前版本** —— 主题是**给四个没有口径的量建立单一事实源**。
> 起因是一份热平衡专项审计:它指出接触器的质量流量取错了状态点,而复核发现根因更深 ——
> `airFlowM3h` 从来没有声明过参考状态,三个产气者各按一套隐含口径,相加后再乘一个密度,
> 本就无解。**守恒断言保证账本自洽,但保证不了记进账本的那个数是对的。**
>
> 分**五批**落地,自检 32 → **35/35**,新增取值来源型断言 R1 / A4 / V1-V4 / W4。
> 13 条数值基线**重设一次**(R24-2 口径变更,`costPerKg` −0.10 ~ +0.36 元/kg,
> 符号随工况方向翻转),其余四批逐位不变。本轮负对照 **20 条**。
>
> - **室外 CO₂ 本底** —— 原是同一物理量的两份口径(热工侧写死 450、工艺侧 350),
>   且后者**无 UI 框**,其钳制从未执行过。现统一为 `co2Outdoor_ppm`,UI 可调。
> - **工艺风量参考态** —— 声明为标准状态 Nm³/h(0 °C、1.293 kg/m³),
>   接触器质量流成为常数,v2.2 的六次不动点迭代整段删除。
> - **风机送进车间的新风** —— K-101 气路是 `OUTDOOR → INDOOR`,它就是新风,
>   此前一分未计(恒温档占通风需求 **36.9%–44.5%**)。现按净额抵扣。
> - **自然通风的供风上限** —— 原来"需要多少给多少":用户填 ACH 1/2/5 结果完全相同,
>   而那份风量**没有风机、零电耗**。现钳在用户填的 ACH 上,不足则报 CO₂ 超标并给出缺口。
>
> ⚠ 计划中的第四批(air 模式默认关塔)经核实后**撤销** —— 该功能 v2.3.3 四批已在
> UI 事件层实现,并由断言 S3 锁住不得下沉计算层。立项时据 `PROCESS_DEFAULTS` 的
> 字面量误判为"默认开塔",已在该行就地留痕防重开。

>
> - **一批 · 字段流转工厂化** —— 治「权威源 → 转发 → 多消费点漏接」这一**已复发五次**的缺陷模式。
>   `SPECIES_FIELD_FLOW` 声明表 + `buildWaterSp` 工厂,使「忘进白名单」成为写不出来的代码;
>   断言 M15a–d 用**静态源码扫描**(不依赖工况路径,机制性免疫「断言写好但没有工况能走到」)。
>   落地当天咬出同模式第六例 `thermalClass`。
> - **二批 · aerationMode 硬告警** —— 告警由渲染层提升为 `sim.configWarns` **结构化导出**
>   (`AIR_INTENSITY_OVER` / `CO2_OVER` + rootCause 三分)。判据取**结果级而非配置级**、
>   **零新阈值**;o2 关塔文案升格为「物理上必然积累,o2 模式必须启用脱气塔」。
> - **三批 · 空气曝气密度分界 40 → 30** —— 并更正本库自己的引用错误:GSA 原文是「区间 30–40 + 硬上限 40」,
>   旧注释把硬上限抄成了分界线。⇒ 立为纪律:**引用外部数字须连同原文口径一并记录**。
> - **四批 · 加载路径哨兵盲区** —— `ensureStageProc` 以「默认值是不是数字」为判据,
>   使哨兵字段(默认 `null`)**整类漏在防线外**;判据改为「FIELD_SPEC 表内有无条目」。
>   ⇒ 纪律:**防线的判据要对准「表」,不要对准「值的类型」**。
> - **五批 · 空气曝气设计工况点** —— 补上 `designDO_mgL`,与 `co2TargetMgL` 成对称一对。
>   此前 DO 取 `DOmin`(安全下限)、CO₂ 取鱼种阈值(安全上限)——**两侧都拿安全红线当设计目标**,
>   系统常年零裕量运行。留空回落原值,故基线逐位不变。
> - **文档批 · 手册第 15 章与参数速查表同步** —— 15.2 密度分界随代码改 30、
>   **「微孔盘 SOTE 18%」清水常数退役**换成完整 OTE 公式;15.3 标注仅适用纯氧拓扑;
>   新增 **15.3b 空气曝气风量选型与设计工况点**、**15.3c 空气曝气要不要上脱气塔**;
>   参数速查表 D 组补登五个曝气参数。
> - **风压建模批 · K-101 风压改为与池深关联**(项目方现场指出)—— 原写死 **70 kPa**
>   且与池深无关,而 70 对应约 **7 m 深池**;空气曝气按模型自身约束**只用于 1.0–2.5 m 浅池**,
>   7 m 深池只出现在三文鱼成鱼期(o2 模式,不用 K-101)⇒ **这个常数被设定成了一个
>   它服务的工况里从不出现的值**。全库 air 模式风机功率高估 **2.84 倍**(989→391 kW),
>   而 K-101 电耗进 OPEX、风机 CAPEX 按元/kW 计价 ⇒ **运行与投资同时高估**。现按
>   `9.81 × 池深 + 盘阻管阻`(默认 10 kPa,四源检索收敛),盘阻做成 UI 可调并标注**待校准**。
> - **清账批 · 手册 15.6 重写 + 33 处死 fallback 清零** —— v1.7 旧公式降级为存档并注明
>   「照它算会把风机选到实际需要的 1/3–1/2」;33 处 `stage.proc.x : 字面量` 改读
>   `PROCESS_DEFAULTS`。⚠ 后者是**永不触发的死代码**,常规门禁给的绿是空绿,
>   靠"清空 proc 强制走 fallback × 五鱼种渲染 DOM 逐字符比对"才证得等价。
> - **案例章生成批 · 23–25 章与成本构成表改为模型实算生成** —— 基准工况取门禁基线
>   `salmon_bergen_w`,生成值与基线期望**四项逐位吻合** ⇒ 案例章从"最易过期的一章"
>   变成被基线门禁顺带保护的一章。其闭合校验顺带咬出 **`hvacCostDaily` 参与合计求和
>   却从未导出**(占 4.6%)——又一例"合计对、明细少一项、对账对得平"。
> - **表格生成批 · 六张手抄表退役** —— 逐章复核三轮发现:错得最狠的不是公式,是
>   **把代码里的表抄成手册里的表**(五张表无一幸免,且多因"硬凑成矩形"而错位)。
>   现由 `gen_manual_tables.js` 从源码生成,**值由代码出、释义由人给**。
> - **告警迁移批 · 24 条阶段告警从"导出即消失"变为进可研报告** —— 判据本就是纯函数,
>   故做**忠实等价拆分**(判定/渲染分离),一次性全部进导出面并由报告 15.5.1 节披露。
>   W3 静态锁另加一条:**渲染层不得出现 `warns.push`**。
> - **下游审计批① · 告警链打通到可研报告** —— 审计发现四组告警/曝气量**根本不在下游
>   payload 里**,于是"报告照出成本、不带任何不可行标记"是字面成立的。打通 index 过桥
>   (断言 Y0–Y3)→ report 消费 → **第 16 章结论改条件式**(检出严重告警不得再写"完全可行")。
> - **版本口径批 · 下游页版本号跟随主页面** —— 四页此前四个口径(设备清单 v1.3、
>   P&ID v2.0、报告 v1.7、财务**根本没有版本号**),现全部对齐 **2.3.3**,各立
>   `IRAS_PAGE_VERSION` 单一事实源。**有意不跟随两类**:`pidSchema` 等**数据契约版本**
>   (表达"能读哪些格式",跟着改会破坏向后兼容)与注释里的**史料标注**。另补上此前
>   四页都没有的「方案版本 ⇔ 页面版本」不一致提示。docver 登记表 10 → **19 项**。
> - **下游审计批② · 设备清单少四台、总价却对得平** ——
 equipment.html 的行是硬编码的,
>   缺 `K-501`/`DH-1001`/`AHU-1002`/`KV-1003` 四台;而 finance/report 用聚合值,
>   **钱一直在里面、对账对得平**,只有拿清单去询价才会发现少买了设备。改为**自
>   `capex.items` 反向补全**(新增设备自动出现)。另修 pid.html 冻结四个版本的内嵌示例
>   (`irasVersion 1.1.0`,补上从没写出来的注入工具),并让 K-101 / KV-1003 两行
>   **自己说清"为什么是这个规格"**(选型控制侧 / 通风控制项)。新门禁 C6/C7/C8。
> - **下游审计批① · 告警链打通到可研报告** —— 审计发现四组告警/曝气量
>   **根本不在下游 payload 里**(此前记的是"报告侧消费未做", 实际严重一档),
>   于是"报告照出成本、不带任何不可行标记"是字面成立的。本批打通 index 过桥
>   (断言 Y0–Y3, 逐位相等判据)→ report 消费 → 报告呈现(**15.5 设计边界与模型
>   告警**节 + 第 5 章就地标注 + **第 16 章结论改条件式: 检出严重告警不得再写
>   "完全可行"**)。取向是**显式披露而非硬阻断**。新门禁 `check_downstream_contract.js`。
> - **收尾批 · 两条遗留清零** —— ①**脱气塔默认值三处文档与代码不符**:v2.0 对标
>   Timmons Ch.10 改了 `co2StripperGtoL` 3→5、`co2StripperLoadingRate` 40→80、
>   `co2PackingHeight_m` 1.0→1.5,三处文档都没跟,用户照文档填 3 而模型按 5 跑。
>   一并咬出 `denitriFlowPct` 文档写 10 / 实际 2。新增门禁 `check_defaults_sync.js`
>   直接对准 `PROCESS_DEFAULTS` 这张表。②**`air + 开塔` 入室 CO₂ 少算已修**(见下)。
>
> ✅ **上一版遗留的 🔴 缺陷「air + 开塔时入室 CO₂ 少算」已在本版修复**。
> 缺陷原貌:曝气吹脱的 CO₂ 在**水侧不记移除、室侧不记入室**,两头都不记 = 孤儿项
> (与 v2.0 `Q_air_gain`、v2.1 蛋分气流、v2.2 K-101 轴功同族)。方向是**激进而非保守**——
> 少算入室 CO₂ ⇒ 少算新风 ⇒ **通风机选小 1.76 倍**。修法**不是补第三档特判,而是取消特判**:
> 入室份额由质量守恒导出 `fracAer×sAer + (1−fracAer)×degasFrac`,o2 与 air+关塔 两档
> 退化为原值逐位不变,air+开塔 那一档得到修正。补断言 X0–X3 与锚定工况
> `edge_air_stripper_on`,自检 29 → **31**,负对照 6/6,13 条基线逐位不变。

> **v2.1（2026-08-01）** 源于**第三方第四轮审计**。修复 v2.0 架构重构时留下的
> **能量守恒破口** —— 删除空气节点时，挂在该节点上的 `Q_air_gain_kW`（电机/灯管废热）
> 与除湿冷凝热成为孤儿项，既不在水侧也不在暖通侧，带走 HVAC 显热负荷的 **41.8%**；
> 并补齐审计建议三轮、唯一没落地的**守恒类断言**。
> 另修自然冷却可见性回归、四处口径/参数、鱼种默认气候联动。
> 13 条数值基线全部重算。详见下方 v2.1 章节。

> **v2.0.1（2026-08-01）** 是补丁版，**不改动任何计算模型** —— 13 条数值基线逐位不变。
> 修复模块化聚合 `totalKw_field` 对流量定额设备产出 NaN（潜伏缺陷，下游未消费）；
> 消除容积率 FAR 默认值 0.5 / 0.7 两个口径并存（存盘与导出不一致，可致总占地静默缩小 29%）；
> 页脚与版本面板改为从 `IRAS_META` 注入，并新增自检项锁定版本号一致性。
> 自检 16 → **17 项**。详见下方 v2.0.1 章节。

> **v2.0.0（2026-07-31）** 是自 v1.2 引入双节点热平衡以来**最大的一次模型改动**：
> 车间温度由「自由漂浮的因变量」改为「暖通维持 = 水温」的输入，删除双节点求解器
> （热平衡代码**净减 65 行**）；**首次计入气液接触器**（CO₂ 脱气塔 + MBBR 曝气，
> 96,287 m³/h，为机械新风的 4.2 倍）的热湿交换；**暖通显热首次计入能耗**；
> 脱气塔默认值对标 Timmons Ch.10；修正鱼种参数库三处物理不可行的组合；
> 基线工况按「鱼种 × 地区」实际配对重设，新增热带三文鱼工况。
> 自检 14 → **16 项**，13 条数值基线全部重算。详见下方 v2.0.0 章节。

## v2.4 → v2.5 ⭐ 最新

### v2.5:沿程口径归位(2026-08-23 · 第八轮审计 · 十四批)

本版主题只有一件事:把几个**算在了错误位置**的量放回它该在的节点。
外部审计七条 + 复核追加三条。**自检 35 → 42;13 条数值基线两次有意变更,其余批次逐位不变。**

> **本版最重要的一句话:集总口径下算出来的那个数,未必是它标签上写的那个点的值。**
> 与 v2.4 那族「取值来源型断言」治的是同一个病 —— 这次是**取值位置**。
> 守恒断言依然全绿,因为账本两边同步变化;**标签错了,守恒式看不见**。

**① 沿程七个节点的 pH / Ω / NH₃ 是从鱼池复制的。** 逐节点展开只覆写各自改变的浓度,
五个碳酸盐字段原封不动。脱气塔节点 CO₂ 降 65%,真实 Ω 由 0.75 升到 1.52(**跨过饱和线**),
而界面上七个节点全标"欠饱和"。重算需该节点的(碱度, CO₂)**两个**量——碱度沿程不守恒。

**② CO₂ 稳态式算出来的不是鱼池,是脱气塔进水。** 呼吸产 CO₂ 在鱼池、硝化产 CO₂ 在生物滤池,
而后者位于鱼池下游、脱气塔上游。链式重导给出 `C_塔前 = Δ_总/(η + η_换水)` ——
**恰好等于原集总式的值** —— 与 `C_鱼池 = C_塔前 − δ_n`。即**原式算的量,标签贴错了地方**。
鱼池 CO₂ 降 15–39%。按**双报**落地:福利判据取鱼池值,设备选型取塔前值 ——
原实现把两个工程量混在一个数里,**用鱼的限值去卡一个鱼接触不到的浓度**。

**③ MBBR 曝气风机按 DO=0 选型,而实际驱动力几乎为零。** 标准氧转移效率的定义条件是
清水 20°C **溶解氧为零**;实际池内 5–12 mg/L,实算效率仅 **0.35–0.5%**。
新口径把因果倒过来:风量由**载体流化**决定(Norsk Vann 导则 10–15 m³/h·m² 池底),
溶解氧由质量平衡**算出来** —— 解是两端点的加权平均,**数学上跑不出窗口**,不需要钳制或地板。
冷水鱼种电耗 +1.9~2.4%,暖水 **−3.4~−4.6%**(原按清水口径虚高三倍多)。

**④ 五处"某个量算了一半"。** 本版十四批里有**五批**是同一模式——一个量在这个账本里有、那个账本里没有:
碱度收支缺排水出口项(碳酸氢钠低估 5–35%)· MBBR 曝气吹脱 CO₂ · 生物滤池异养呼吸产的 CO₂
(实测占滤池产量**四成**)· 生物滤池对溶解性 COD 的去除(算了耗氧、算了产 CO₂,**唯独没扣底物**)·
气相载量型效率未计碳酸盐缓冲。
⇒ 反复到第五次才立**白名单断言**:凡接触水的气流,要么进 CO₂ 分解,要么显式登记为「有意不计 + 理由」。
**它不判"不计是否正确",只判"有没有做过这个决定"**。

**⑤ 淡水方解石饱和度:四个鱼种从"算不出"到"方向还反了"。** 钙按海水比例反推 ⇒ 淡水恒为 0,
**四个淡水鱼种全场 n/a**。开放钙硬度输入后,未加活度修正的实现给 Ω = 1.22–1.76(过饱和、会结垢),
而 Langelier 饱和指数独立算出 0.40–0.70(欠饱和、会溶蚀)——**方向相反**。
而"溶蚀混凝土池体"正是本模型该提示的问题,**不修正等于把它藏起来还反着说**。

**⑥ 外部口径交叉验证。** 新增两条拿**库外通行量**来对的断言:缓冲因子换算成
**Revelle 因子**须落在 8–15(实测 13.9);淡水 Ω 须与 **Langelier 指数**同侧。
库内多数断言查的是内部自洽,而**内部自洽挡不住整套实现一致地偏离物理** ——
⑤ 那版方向反了的实现,只跑内部断言会**全绿通过**。

**⑦ 两条流程门禁。** 交接台账的门禁基线本版**过期六次**,而没有任何门禁在看台账
(版本检查查的是**一致性**不是**新鲜度**);新增台账门禁后其中三次是被工具抓的。
同期发现 logo 有**四份拷贝**,而首版门禁只锁了一份——照那样收工,换 logo 后页头两处仍是旧图而全绿。

**遗留**:生物滤池模型重构已立范围书(硝化面速率动力学 + 溶解性 COD 按降解性拆分 +
字段改名 + 淡水蛋分默认值,**四件事必须同批**)。三个待标定系数中 `bfAlpha` 是唯一进成本链的。

---

## v2.3.3 → v2.4
<!-- IRAS_V233_README_REBUILD: ⚠ 本章为【补齐重建】。真 v2.3 README (MD5SUMS 记录
     dc56b923…) 不可得, 本 README 以 v2.2 副本为底, v2.3 全部内容自 index 工程
     记录 / manual 版本演进章 / HANDOFF_v2_3_20260813 搬运压缩而来 —— 内容有出处,
     非事后追述。 -->

### v2.4:四个量的口径单一事实源(2026-08-18 · 五批)

起因是一份**热平衡专项审计**:它指出气液接触器的质量流量取错了状态点(用了风机加热后的
密度)。复核后发现根因更深——`airFlowM3h` **从来没有声明过参考状态**,三个产气者各按一套
隐含口径,相加后再乘一个密度,**本就无解**。

> **本轮最重要的一句话**(引自审计原文):
> **守恒断言保证账本自洽,但保证不了记进账本的那个数是对的。**
>
> 无论取哪个密度、扣不扣哪股风,守恒式两边都会同步变化、残差恒为零——这类缺陷
> **守恒断言天然抓不到**。本版因此新增一整族「**取值来源**」型断言(R1 / A4 / V1–V4 /
> W4 / G0–G4 / U2),它们不问账本平不平,只问这个数**是从哪儿来的**。

| 批次 | 内容 | 自检 | 基线 |
|---|---|---|---|
| R24-1 | 室外 CO₂ 本底统一为 `co2Outdoor_ppm` | 32 → 33 | 逐位不变 |
| R24-2 | 工艺风量声明标准状态 Nm³/h + 接触器质量流常数化 | 33 → 34 | **重设 52/52 项** |
| R24-3 | 风机入室空气计入通风,净额抵扣 | 34 | 逐位不变 |
| ~~R24-4~~ | ~~air 模式默认关塔~~ | — | **撤销** |
| R24-5 | 自然通风钳制 + 三条通风告警 | 34 → **35** | 逐位不变 |
| R24-6/7 | 版本号 v2.4 + 文档 + 门禁扩到全局配置层 | 35 | 逐位不变 |

**① 室外 CO₂ 本底——同一物理量的两份口径。** 热工侧写死 `co2Out_ppm = 450`(无 UI、
无默认表条目、无范围登记),而 `PROCESS_DEFAULTS.co2IntakeCO2_ppm = 350` 是同一物理量的
另一份,**值还不一样**。后者更麻烦:它有读取路径且带 300-5000 钳制,但**根本没有输入框**,
那句钳制**从未执行过**。现统一为 `co2Outdoor_ppm`,两个消费点一律经同一 resolver;
钳制放 resolver 而非读取函数——方案 JSON 直写全局配置会绕过 UI 约束。
⚠ 撤字段时把 Fig 10.17 论证**迁存**到脱气塔气路注释:**字段可撤,依据不可撤**。

**② 工艺风量——标准状态 Nm³/h。** `1.293 kg/m³ × 0.232 = 0.300 kg O₂/Nm³`。理由:对外
向风机厂家提规格时给的就是 **Nm³/h + 压力**,质量由该单位本身锁定,故风量/压力/功率
全部同口径、**不做进气温度修正**。接触器质量流因此成为**常数**,v2.2 那段六次不动点迭代
**整段删除**——它当年确实统一了两侧密度,但统一到了"风机加热后的密度"这一侧,而
**风机不改变质量流量**。
⚠ 全库 `1.2` 共 9 处**只改 3 处**(Lewis 类比的车间空气密度、除湿/暖通选型裕度、通风显热的
0.34 **均不得改动**);`0.23` 与鱼种耗氧系数数值相近而物理无关。**禁止全局替换**,由断言 A4d
**反向保护**(不该改的必须仍在原位)。

**③ 风机送进车间的那股空气——它就是新风。** K-101 鱼池增氧鼓风机**没有排气风管**:吸室外
空气、气泡穿过池水、从池面进车间。此前**一分未计**,两笔账同时错——CO₂ 稀释侧让通风系统
重复提供了这部分;显热侧这股空气已被池水加热到水温饱和(**水侧付过了**),通风显热又收了
一遍。实测在恒温车间占通风需求 **36.9%–44.5%**,自然通风档 4.3%,纯氧拓扑 0。
⚠ **不新增"超额排风热损"项**:该气流在自由空气节点求解器里已作为独立股入解,再加一项即
把它的焓算第二遍——那是 v2.3.3 收尾批"孤儿项"的镜像。

**④ 自然通风——从"需要多少给多少"改为钳在设定 ACH 上。** 此前用户填 ACH 1 / 2 / 5
**结果完全相同**(室温 8.4 °C · 车间 1500 ppm · 25.03 元/kg),因为 CO₂ 需求一直顶着,
**用户的输入被架空**;而那 35,414 m³/h **没有风机、零电耗、零 CAPEX**,等于"选自然通风就能
免掉通风机"。物理上站不住:自然通风由风压/热压驱动,**不会因为室内 CO₂ 高就自己变大**,
且随开口面积与天气跨近两个数量级(温室夏季敞开 45–60、冬季关口约 3)。
现钳在设定 ACH 上,不足则**让车间 CO₂ 真的算超标**并给出缺口与两条出路。
实测 ACH 1 → **4422 ppm**、2 → 2894、5 → 1584、15 → 857、30 → 658——
**用户填的 ACH 终于有意义了**。

**⑤ 三条通风告警,与一条被否决的候选。** `ROOM_ACH_TOO_LOW`(warn,阈值 15 取自该档自己的
定义注释「简易棚取 15–30」,**零新常数**)· `NAT_VENT_SHORTFALL`(warn)·
`VENT_CTRL_BY_CO2`(info,沿用已有标志)。
⚠ 原遗留清单里的「**车间 CO₂ 超限告警**」**判据恒假,已改写**:通风量本就是"恰好把浓度压到
限值"的反解,故 CO₂ 一旦成为控制项,车间浓度**恒等于限值**。照原样写会得到一条永远为假的
判据、负对照全绿。
⚠ `VENT_CTRL_BY_CO2` 的措辞里写明**不要为了缩小通风机而开脱气塔**——实测在空气曝气下
开塔使全场能耗上升 **27–93%**。这条拦住的正是"只看通风机风量降了 71%、没看总账"这个推理。

**⑥ 门禁扩到全局配置层。** 默认值同步门禁的三条判据此前**全部对准阶段工艺参数表**,于是
全局热工配置层**从来没有门禁**。新增 G0–G4(判据 136 → **167**),上线即咬出两条 WARN:
`co2IndoorLimit_ppm` 的 UI `min=800` 与代码下钳 `600` **分居 HTML 属性与代码字面量**;
全局默认里的围护五项**逐值等于「一般工业车间」预设**。
断言 **U2** 关掉了 U1 的结构性盲区:U1 是从"有框"一侧起判的三件套,对
「**无框 + 有读取路径 + 有消费点**」这一类**结构上判不到**。

**⑦ 基线变动只有 ② 一处,且符号随工况方向翻转。** 52/52 项,`costPerKg`
**−0.10 ~ +0.36 元/kg**:制热工况下降(青岛冬 −0.102 · Bergen −0.013),制冷工况上升
(海南夏 +0.358 · 三亚 +0.255)。与 v2.1/v2.2 守恒修复一致——**单向偏移才说明修错方向**。
⚠ **不要引用"最大相对差 13.68%"**:那一项绝对值仅 +0.024 元/kg,是小分母造成的假象。
**引用变动幅度时必须看绝对值。**

**⑧ 一个被撤销的批次,与六次同类错。** 计划中的「air 模式默认关塔」**核实后撤销**——该联动
v2.3.3 已在 **UI 事件层**实现并由断言 S3 锁住不得下沉计算层;立项时**据一处默认值的字面量
推断了系统行为**,没查 UI 事件层。
本轮在**六个不同位置**犯了同一形态的错:为判据或探针挑对象时**用了记忆里的名字而不是先查**。
其中两次是**恒真型**(断言宿主函数填错 ⇒ 写了、跑了、全绿,但从未判过任何东西),若不跑
负对照就会随本版一起交付。⇒ 固化三条纪律:

- **纪律 18**:静态断言必须显式声明宿主并核实存在,判据取被判对象自身结构而非旁证字段,
  **找不到目标一律判红**。
- **纪律 19**:探针读数出现「**一半对一半空**」时,**先怀疑量具**。
- **纪律 20**:引用变动幅度时**必须看绝对值**。

⚠ 另有一条**判据本身写错**的教训:新门禁 G1 第一版写成"UI 范围必须等于消费点钳制",于是把
合规的 800/600 判成红——而既有设计本就是「UI 引导范围」与「模型接受范围」分开存,二者
**允许不等**。差一点逼出一个错误的"修复"。**判据错比代码错更贵:它会让人去改对的东西。**

**⑨ 版本号命名空间:两套编号并存。** 代码里的 `IRAS_V24_*` / `IRAS_V25_*` / `IRAS_V26_*` 是
**内部开发批次号**,与发布版本号**不是同一序列**——那三批成果都随 v2.3.x 发布,从未独立发版。
故本次发布批的新哨兵一律 `IRAS_R24_*`。旧哨兵**一律不改名**(它们是史料;本库有过全局替换
污染历史叙述的先例)。

**手册对应章节**:15.6b(风量参考状态)· 15.9(本轮已决取舍六条)· 28.7(通风净额与自然
通风上限)· 版本演进章 `v2.3.3 → v2.4` 节。

---

---

## v2.3 → v2.3.3

### v2.3.3 四/五批:空气曝气设计工况点 + 加载路径哨兵盲区(2026-08-13)

**加载路径防线判据错位**:`ensureStageProc` 以「默认值是不是数字」决定钳不钳,
哨兵字段(默认 null)的 typeof 是 object,**整类漏在防线外**——注入
`co2TargetMgL=0.0001` 使关塔风量 4,239 → **12.7 亿 m³/h**,与第七轮审计 P1 的
`co2StripperGtoL=1e6` 同族同路径。判据改为「FIELD_SPEC 表内有无条目」,空值仍
保持 null(未设定 ≠ 设定为下限)。断言 S1/S2 构造注入复验,不依赖工况。

**两侧都拿安全红线当设计目标**:CO₂ 取鱼种阈值(上限)、DO 取 DOmin(下限)——
同一毛病的两面。补成对称一对:**设计 DO + 设计 CO₂**,留空回落鱼种值(基线逐位
不变)。控制侧可观测翻转:留空时 CO₂ 侧 4239 险胜 O₂ 侧 4228,设计 DO 提到 6.0
立翻 O₂ 控制(7243),提到 7.0 达 15993(近四倍)。

**池内曝气与循环流量无关**(产业侧指出的拓扑差别,已实测):turnover 1→4(流量
4×)时 air 模式 DO 恒为 **4.9813 纹丝不动**,而 o2 模式同一扫描 DO 由 0 → 8.39。
⚠ 二阶耦合如实记录:关塔 CO₂ 稳态随流量小幅变(23.7→27.3),系经风量的间接效应。

**air ⇒ 脱气塔默认关**:罗非1000t 实测开塔 31.25 元/kg·14.23 kWh/kg,关塔
**26.20·8.35**(省 16%),CO₂ 27.3 < 阈值 30 守得住;代价是 CO₂ 转入车间,
ACH 1.0 → **6.05**(恒温恒湿车间需掂量)。联动**只在 UI 事件层**(计算层猜意图
会静默改掉用户显式配置并动基线),用户手动过开关后不再被覆盖,断言 S3 静态锁。

**教训**:给 `co2TargetMgL` 加输入框后随即发现 `readStageProcFromInputs` 根本
没读它——**UI 加了但白加**;S4 断言因此同时查"留空须回落"与"填了须生效"。

### v2.3.3 三批:空气曝气密度分界 40 → 30(2026-08-13)

产业侧质疑 UI 的 40 kg/m³ 偏高。查证不仅确认判断,还**更正了本库自己的一处引用
错误**:注释记「GSA >40 必须纯氧」,原文实为**区间 + 硬上限两句**(仅靠曝气典型
限于 **30–40**;>40 则耗氧超出曝气能力)。旧注释把硬上限抄成分界线,UI 据此写成
"<40 都可用空气"——**把区间上沿当成了推荐值**。新分界 **30** 取自 **SRAC-453**
(美南水产中心 RAS 设计实践综述:密度 <30 kg/m³ 时气提可提供全部需氧,满密度
60 时仅约一半);⚠ 该装置为开放池气提(效率约 0.80),与本模型池内微孔盘不完全
同源,已标明。另有 TheFishSite「>50 才常规上纯氧」更宽松,**但答的是经济性问题
而非工程能力问题,不并列取值**。**口径分层**:30 进 UI 与参数表(物理上限)·
20–25 进选择器提示与注释(DB46/T 424—2017 + 设备商参数 + 模型反算三源收敛,
国内浅池实配)· 任一阶段准确上限始终以 `aerIntensity` 反算为准。纯文案/注释改动,
基线逐位不变,28/28。**教训**:根因不是数字取错,而是抄数字时丢了口径
(范围/硬上限/推荐值)——失去口径的数会在传递中变成另一种含义。已立为纪律:
**凡引用外部数字,须连同原文口径一并记录**。

### v2.3.3 二批:aerationMode 硬告警(2026-08-13)

处置遗留"密度超空气曝气承载/o2 关塔仍静默算出成本"。侦察修正问题定义:渲染层
**已有**曝气强度与 CO₂ 超标告警,真实缺口是告警**困在渲染层**(无导出面、无断言,
渲染改坏无人知,报告照出成本不带标记),且 o2 关塔这一"物理上必然积累"的配置与
"塔效率不足"共用一句轻文案。落地:`sim.configWarns` 结构化导出
(`AIR_INTENSITY_OVER` + `CO2_OVER` 带 rootCause 三分;**结果级判据、零新阈值**,
全部复用水质链已算出的量)· 断言 W0–W2 锁数据层(双向 ⇔ + 双工况锚定,预飞探针
`probe_warnmatrix.js` 事先实测触发矩阵)· W3 静态锁渲染挂钩(防数据层↔渲染层
双源复活)· 渲染改吃单源(o2 关塔文案升格为"o2 模式必须启用脱气塔")·
参数表联动(aerationMode 选择器着色 + 可行密度上限提示;**有意不做自动切换**)。
红→绿全程:断言先行 FAIL 3/27 → 数据层落地 **28/28**;负对照 3/3 精确转红;
13 条基线逐位不变。顺带发现 `edge_yield_tiny` 处于 CO₂ 21.5–23.7 略超阈值 20 的
边界超标状态——结构化导出上线第一天就多看见一件此前无人知道的事。

### v2.3.3:字段流转工厂化(2026-08-13)

治「权威源 → 转发 → 多消费点,某个消费点没接上;各自内部自洽故断言全绿」——
该缺陷模式已复发**五次**(DOmin_* / recDepth_m / isCrustacean / 通风机风量口径 /
曝气侧池深不同源)。案发现场是 `recomputeAll` 的手写 sp 白名单:每漏一个鱼种字段
就多一处静默兜底。本版:`SPECIES_FIELD_FLOW` 声明表(每字段声明 scope/toSp/resolve)
+ `buildWaterSp` 工厂生成白名单,"忘进白名单"成为写不出来的代码;池深解析收敛为
`resolvePoolDepth` 全库唯一副本(曾三副本且兜底不一致 3 vs 1.5)。断言 **M15a–d
静态源码扫描**(`.toString()`,不依赖工况路径——机制性免疫 M13/M14 两次
"断言写好但没有工况能走到"的陷阱):消费未声明 / 死转发 / 手写字面量复活 /
设备侧绕过工厂,四类当场咬。**断言落地当天咬出同模式第六例 thermalClass**
(消费代码支持鱼种显式声明温冷水类别、白名单从未转发,只因无鱼种声明才没踩响)
与 protein 死转发,均已处置。设备侧曝气选型改吃同一工厂产物——M11 从"事后比对"
升级为"结构上单源"。纯结构重构,**13 条基线对【v2.3 新基线值】逐位不变**,
自检 26 → 27。负对照 4/4 精确转红(其一并级联咬中 M11/M14,双层防御实证)。

### v2.3 内六批(2026-08-12/13 会话,空气曝气 + 简易/通风车间主线)

由产业侧提问驱动,把模型从"只能表达恒温车间 + 纯氧"扩展到国内主流技术路线:

- **简易模式(自然通风)**:`solveRoomAir` 自由空气节点求解器 + 太阳辐射子系统
  (九气候区 GHI + 四档围护 SHGC,全库首次)。此前该场景**无表达路径**
  (hvacEnabled=false 只是不收电费,A3 仍强制室温=水温)。断言 R1–R5。
- **池内曝气拓扑修正**:air 模式 DO 稳态由「传质=耗氧」池内质量平衡决定,与循环
  流量无关——原用主流增氧式(进池浓度−单循环降幅),而 air 的气直接打进池子,
  根本没有"进池浓度"。修正前密度 40 苗种期算出 DO=0.00(拓扑用错的伪值,
  非物理结论,特此存档防引用),修正后 5.31。断言 M9–M11。
- **车间模式三档 + 通风机**:conditioned / natural / mechanical,分档判据只有
  一条——能否自动调节温湿度;补一台两档共同缺失的车间通风机 KV-1003
  (功率+CAPEX+登记)。**13 条基线因此有意重算**(先出预测表:全部 +0.01~+0.08,
  实测方向 13/13、量级 13/13 命中)——即【v2.3 新基线值】。断言 M12a/b。
- **CO₂ 脱除机制澄清**:确认 O₂ 侧 OTE 含水深 / CO₂ 侧不含**是正确设计**
  (O₂ 溶入速率控制 vs CO₂ 气相载量控制,气泡出液前已饱和)——照"补上水深依赖"
  去改反而改坏。真问题在浅池(饱和前提是停留时间够):新增浅池告警,只告警
  不折减(转折点出自搅拌式反应器,不同源数字不当阈值)。
- **曝气强度限值校准**:池底 1.7 / 体积 2.5 m³/(m²·h)(鱼类),甲壳类 4.0/3.5
  (工厂化对虾正常工况即"水面沸腾状");新增 `isCrustacean` 分档。断言 M13。
- **成文标准 + 池深 + 审计**:DB46/T 424—2017《豹纹鳃棘鲈工厂化养殖技术规程》
  入库(成鱼期 ≥20 kg/m³ · 池深 1.0–1.5 m · DO≥5 与模型逐字相符);石斑成鱼期
  recDepth_m 3.0→1.5(仅 grouper_500 基线重算 +0.21 元/kg,预测命中);审计查出
  **曝气侧池深不同源**(用户改池深曝气强度纹丝不动——"修一半比不修更隐蔽")。
  断言 M14。**该会话最重要发现:同一 bug 模式复发五次**,治本方案(工厂化)
  列为下版第 0 位——即 v2.3.3 所做。

产业参数资产:空气曝气承载量四源收敛 20–25 kg/m³;2026-06 海南出塘价
(珍珠龙胆 18.6–22 元/斤,倒挂;东星斑 61–65,有空间)——财务模型必须用出塘价,
产地/批发报价是另一套口径。

### v2.3:第七轮审计处置 + 曝气风量物理选型(2026-08-10)

**第一批(审计处置)**:P0 `<head>` 被三处多余字符提前闭合(20 个 meta 掉进 body,
爬虫全部失效而 querySelector 断言照绿)→ 修复 + head 结构断言;P1 方案 JSON
加载路径绕过全部钳制(co2StripperGtoL=1e6 → 成本 109 万元/kg)→ ensureStageProc
兑现不变式 + `edge_hostile_json` 工况;P2 鱼池曝气尾气入室(池面破裂必入车间,
罗非 +336 kg/d 湿负荷,审计/模型/手算三方吻合);P3 全局热工配置三处副本收敛
(degasserExhaustIndoorFrac 已实际漂移)+ 键集锁定断言;chart.js SRI 落地
(含对 v2.2 注释的事实更正:原 URL 的 .min.js 在 npm 包内不存在)。

**第二批(曝气风量物理选型,海南主案例驱动)**:旧 air 风量 = O₂峰值 ÷
(1.2×0.23×SOTE 0.18),清水标准态、无水深/αβ/θ/驱动力修正,系统性低估 2–4×;
CO₂ 侧完全缺席(关塔时 fallback 旋钮与风量不联动,海南石斑关塔实测 CO₂
55–179 mg/L 超阈 2.5–8 倍而选型侧毫无反应)。实装 `tankAerationDesign`:
OTE = SOTE(%/m)×水深×αβ×θ^(T−20)×驱动力;CO₂ 侧 Henry 平衡载量(**复用碳酸盐
模块 K0_W74 与 ρ,零副本**);风量取 max 并标注控制侧;**关塔稳态 η 由风量反算**
——选型与稳态共源。断言 M8 红→绿:旧模型 144–179 mg/L 转红,实装后 20.5 转绿。

**交付方式变更**:v2.2 的 patches/ 不可得,自 v2.3 起直接在交付产物上编辑,
"全量重建"可复现性封存;验证工具按 HANDOFF 规格重建。编辑纪律不变
(replace_once 唯一命中 / 断言先红 / 基线先预测)。

### v2.3 新基线值(13 条,v2.3.3 全程逐位不变)

⚠ 与最初 v2.3 交付版基线**不同**(车间通风机批次有意重算),以 MD5SUMS 区分。
下方各历史章节中的旧基线表为**各自年代的历史记录**,对比数字前先确认版本。

| 工况 | 工艺成本 元/kg | 全成本 元/kg | 比电耗 kWh/kg·yr | 热泵费 元/kg |
|---|---|---|---|---|
| salmon_qingdao_annual | 40.37 | 45.51 | 12.24 | 1.14 |
| salmon_qingdao_w | 38.89 | 43.96 | 11.89 | 1.07 |
| salmon_hainan_w | 40.11 | 45.25 | 13.36 | 1.69 |
| salmon_hainan_s | 44.16 | 49.56 | 19.31 | 4.65 |
| salmon_bergen_w | 37.70 | 42.69 | 10.20 | 0.18 |
| salmon_sanya_annual | 42.84 | 48.14 | 16.87 | 3.46 |
| tilapia_1000 | 30.93 | 35.58 | 9.76 | 0.79 |
| turbot_500 | 56.23 | 66.42 | 16.53 | 1.75 |
| grouper_500 | 44.83 | 54.54 | 14.34 | 1.74 |
| bass_300 | 46.43 | 53.39 | 12.55 | 1.05 |
| eel_200 | 61.03 | 116.99 | 12.75 | 1.00 |
| mandarin_200 | 58.68 | 70.53 | 16.15 | 1.37 |
| shrimp_100 | 50.49 | 67.58 | 17.62 | 1.90 |

---

## v2.2 发布后持续加固
<!-- IRAS_V22_DOC_STRUCT -->

> 以下三批工作发生在 v2.2 发布**之后**,不改变 v2.2 的功能范围,
> 只增加护栏与修正。按时间倒序排列(最新在前)。
> v2.2 本体的两批发布内容见下方「v2.2 第二批」与「v2.2」。

### 碳酸盐接口一致性 · a11y · CDN 加固
<!-- IRAS_V22_IFACE_README -->

**A. 碳酸盐模块的【接口】验证**(此前只验过模块本身)。主模型与它之间隔着一层
**单位换算**(`TA = 碱度/50043/ρ`、`CO₂ = 浓度/44010/ρ`),那一层从未验证。
`I1` 往返自洽 240 点网格**最大 |ΔpH| = 0**;`I2` 换算层手工复算与模型
**逐位相同(Δ = 0)**;`I3` 独立文件与内联块 20 个函数、63 个平衡常数
归一化 md5 完全相同。真正的风险不是当前是否一致(是),而是**将来会不会漂移**。

**B. a11y**:163 个 label **零个**有 `for` —— 点击标签无法聚焦输入框,
是所有人都损失的常规交互。现关联 65 处;12 个表格补横向滚动包裹。
新增自检三条锁定。📌 实施教训:正则改三轮仍漏 1 处,查明是
**Python `re` 在超长属性上回溯超限后静默放弃匹配**,改线性扫描后一次到位 ——
与 t21 三次扩展全部落空同形态,两次都是**断言先报出数目不对**才发现。

**C. CDN 加固**:Tailwind Play CDN 动态生成响应体,SRI 哈希**无法**预先计算;
chart.js 可加 SRI 但本版**不填哈希** —— 当前拿不到真实值,而**编造哈希会让
浏览器拒绝执行脚本、图表直接消失,比没有 SRI 更糟**。只加 `crossorigin`
并把取真值的命令写进注释。📌 **安全措施也不能靠猜值上线**。

### 物质平衡断言 M
<!-- IRAS_V22_MASSBAL_README -->

补上最后一块无护栏区域。此前断言覆盖能量、水量、登记、资本,
**唯独物质平衡一条没有** —— 而它是本工具的物理核心。
第三方独立复核印证:三条缺陷全部落在无断言区,断言覆盖区一条问题都没有。

- **M1 氧-构成** 总需氧 = 鱼呼吸 + 硝化 + 异养
- **M2 氧-硝化** 硝化耗氧 = TAN × 4.57
- **M3 氧-供给** 液氧耗量 = **鱼呼吸耗氧** / 吸收率(纯氧模式)
- **M4 碱度链条** 净额 = 消耗 − 回收;投加 = max(0, 净额 − 补水带入);药剂 = 投加 / 0.595
- **M6 碳-构成** CO₂ = 呼吸 × 1.375 + TAN × 6.286 × (1 − BF 内脱气)

**负对照复现了历史上真实发生过的 bug**:把过去修过的缺陷形态注回代码 ——
`M6` ← CO₂ 漏硝化项(复核 BUG-2 形态)报 **−41.5%**,与报告实测同量级,
**若 v1.9 之前就有这条断言,那个 bug 根本写不出来**;
`M3` ← 液氧用总需氧而非鱼呼吸(v1.9.2 修过的口径混淆)报 **+104.7%**。

**建立过程的教训**:外部核算这三条口径**连续错了三次**(液氧误用含容量系数的
`o2Demand`、碱度消耗误用 `tanDaily`、碱度回收误用理论 3.57 而非工程值 2.8)。
同族「猜口径」已达十次 —— **这正是要把核算放进产品路径的理由**。

### v2.2 第三方独立复核处置(2026-08-08)
<!-- IRAS_V22_REVIEW_README -->

一份独立复核报告(jsdom 走真实产品路径 + PyCO2SYS 1.8.3.4 / USGS·APHA 溶解氧表 /
ASHRAE·IAPWS 湿空气数据 / 第一性原理手算)提出三条模型误差,
**全部落在本版自述的断言盲区**(氧侧 / 碳侧 / 水汽侧)。逐条实测核实:

- **✅ 采纳** `co2DailyAvg` 漏硝化产 CO₂ —— 只算呼吸缺硝化项,实测少算 34.5%–43.4%。
  设计口径 `co2Daily` 是对的(v1.9 memo F2 修过同一个 bug,设计口径已修而日均未跟上)。
  全库 0 消费点属死字段 ⇒ **删除而非修正**(与删 `sourceTemp` 同一原则)
- **◐ 部分采纳** 0°C 以下饱和水蒸气压 —— 数值属实(−20°C 时水面式高 21.97%),
  但**本项目继续用水面式且自洽**:RH 数据源为 WMO 口径,而 **WMO 对 0°C 以下的 RH
  以水面(过冷水)饱和压定义**,相态前后一致。改冰面式反会使含湿量系统性偏低。
  只订正注释区间为 [0,60]°C 并写明依据
- **✗ 驳回** 海水 DO 饱和度「偏高 3.3%」—— 复核建议「改用 Benson & Krause (1984)」,
  而**代码用的就是该式本身**(12 个测点偏差全部 0.0000%)。报告参考值反解出盐度
  **≈35.5‰**,而对比的是 iRAS 在 **S=30** 的值 —— 差的 3.3% 正是盐度效应本身。
  与第六轮审计的 NH₃/Ω 误报同类:**基准取值与被测对象的工况参数不一致**

**复核通过项**:化学计量 8 项对手算全部吻合;碳酸盐体系对 PyCO2SYS 1400 工况,
淡水 pH 一致到**机器精度 8.88e-16**;能量守恒 B 断言残差 **0.000e+0**。
这些正是断言已覆盖的区域 —— 与「三条缺陷全在盲区」互为印证。

## v2.2 发布内容

### v2.2 第二批:UI 审计 + 字段范围单一事实源
<!-- IRAS_V22_B2_README -->

源于本版**首次 UI 专项审计**。与第一批的登记表工作是同一个模式在 UI 侧的实例:
分散的重复声明必然漂移。自检 20 → **21 项**, 13 条数值基线**逐位不变**(纯重构)。

#### A. 🔴 填 0 → 界面回显默认值 → 用户输入被静默丢弃

完整危害链已在真实事件路径复现。**陷阱在三层**(渲染 31 处 / 读取 / 消费 71 处),
**只修一层反而更糟** —— 实测只修渲染侧造成「界面诚实显示 0、模型仍用默认值」,
比原状态危害更大(原状态至少界面与模型口径一致)。三层同批落地。

#### B. 🟠 FIELD_SPEC:范围的单一事实源

取值约定此前散布四处,**实测已漂移四条**:`co2StripperEff` 三处三个值(65/65/75)、
`co2StripperGtoL` 事实源 v2.0 改 3→5 而两份副本停在 3、`co2PackingHeight_m`(1.5 vs 1.0)、
`climateRegion`。这些副本平时是**永不触发的死代码**,故漂移无人发现 —— 直到用户填 0
让它们复活。

FIELD_SPEC 只管范围,默认值仍归 PROCESS_DEFAULTS,由断言 H 保证对齐。
clamp 取 UI 范围与原钳制的**并集** —— 机械规则,不收紧现有能力,无回归风险。
一并消灭:UI 范围 ≠ 代码钳制(7 字段)、22 字段无钳制、同字段多处渲染矛盾(3 处)。

#### C2. 🟠 覆盖扩展 + clamp 规则修正
<!-- IRAS_V22_B2_README_E -->

第一版 FIELD_SPEC 只收了原本带 `min/max` 的 34 个字段,另有 **22 个字段完全无钳制**:
实测 `mainPumpEta=0` → 主泵 1383.6 kW(+6.6 元/kg)、`VTR20=0` → +12.0 元/kg,
**全部有限无 NaN** —— 危害是「看起来正常的错误数字」,会原样进可研报告。
现已全部接入,依据取自各字段自己的 tooltip(22 条无一缺失地写明了工程范围,
只是从未落到属性上)。**至此 66 个输入字段零裸奔。**

**并集规则的缺陷**:第一批的 `clamp = UI 范围 ∪ getNumClamped` 漏了第三个来源 ——
消费处的 `Math.max(x, 下限)`。前两者是**允许区间**(并集=不收紧能力),
后者是**物理硬保护**(必须遵守而非并入)。后果:`co2BlowerEfficiency` 的
clamp 是 [0,100] 而消费处写着 `Math.max(η/100, 0.3)` —— 用户填 0 时
proc 存 0、界面显示 0、**模型实际用 30%**,而 t22 一直报「保真 ✅」。
规则已修正为 `clamp_lo = max(消费处硬下限, 原下限)`,撤除全部 8 处隐式兜底。

**删除死控件 `sourceTemp`**:双证据(全库无消费点 + 填 -99 结果逐位不变)。
源水温自 v1.2 起归全局气候。给死控件加钳制等于承认它还活着,故删而不修。

#### C3. 物质平衡实测(本版仅验证,未建断言)

发布前对四条基本物质平衡做产品路径实测(三文鱼 1000 t):
**氮 0.08% · 硝酸盐 0.00 · CO₂ 0.13% · TSS −1.8%** —— 前三条闭合,
说明稳态方程自洽。TSS 的 −1.8% 未确证为缺陷(更可能是核算漏了排污股)。

⚠ **现有断言无一条覆盖物质平衡**(只覆盖能量/水量/登记/资本)。
即今天闭合是因为公式恰好写对,不是因为有东西防止它写错。
**氧平衡、碱度平衡、碳酸盐体系交叉一致性三项连"今天是否闭合"都未测过。**
已列为下一版首要工作。

#### C. 🟠 断言 H + 判据盲区实录

新增自检项 `field_spec_consistency`。⚠ `t22`(显示保真)检查的量全在 proc 层,
消费侧的 `||` 不影响 proc,**t22 结构上覆盖不了第三层**(负对照实测无反应),
另补静态判据 `t23`。**一条判据绿了不等于该层被覆盖。**

### v2.2:工况覆盖补盲 + 登记表声明自校验
<!-- IRAS_V22_README_REL -->

源于**第三方第六轮审计**。审计核心判断本版照单全收:**断言的质量已超过工况的覆盖面,
下一步的收益在加工况**。自检 17 → **20 项**, 13 条数值基线重算 (|Δ| ≤ 0.02 元/kg,
符号随工况方向翻转 —— 制热 ↓ / 制冷 ↑, 与 v2.1 两轮同款的正确性旁证)。

#### A. 三条配置边界工况 (只跑断言, 不比数值)

13 条数值工况全部「mainline + 脱气开 + 纯氧」—— 本轮两条真缺陷现有断言本可抓到,
只是从没跑到那条路径。新增 `edge_no_degasser` / `edge_bypass` / `edge_air_aeration`,
落地顺序刻意为**先加工况 (转红) 再修缺陷 (转绿)** —— 红→绿转换即工况有效性负对照。

#### B. 🔴 蛋分风机 CAPEX 解嵌套 + ΔT/mdot 不动点

- **CAPEX 嵌套**: 条目嵌在脱气塔 `if` 两层内, 脱气关闭时蛋分照常计费、投资却消失 ——
  F4 目标模式第四次出现, 且在修复它的同一次提交里。现与独立设备并列
- **密度不闭合**: ΔT 按冷密度、焓流按热密度, 丢 (1−mdot/mdot₀) 份轴功
  (默认 1.2%, 脱气关闭 ΔT≈30 K 时 **10.1%**)。现**不动点迭代**至
  mdot·cp·ΔT ≡ P_shaft (mdot 自洽)

#### C. 🟠 轴功 η 逐台加权 + 第四台风机接入 + E2b

- 旧 ΣkW×0.57 与各风机效率参数脱节 (调高效率, 模型反而认为进气的热更少)。
  物理上 kW×η = Q·Δp 与 η 无关 —— **η 逐台加权**自动恢复, η 与各功率公式同源
- 空气曝气的**鱼池增氧鼓风机 K-101** 此前「计费有、CAPEX 有、热去向无、气路无」——
  第四次同族逃逸 (机制: 功率藏在混合数值与格式化字符串的展示对象里)。
  现全量接入登记 (airFlowM3h + airPath + 登记表 + 轴功加权)
- 新增断言 **E2b 递归扫 *Spec 子对象**封住该逃逸类 (aliasPaths 声明已知同值副本);
  治本的 registerDevice 工厂化立项 v2.3

#### D. 🟠 capexVia 精确名称数组 + capexMinKw + 断言 G

- `capexVia` 正则→**精确名称数组** (旧正则假阴性/假阳性双向皆错, bypass 下 F4
  四阶段误报由 edge_bypass 实证); 支路泵显式声明共享聚合条目
- 精确化曝光被掩盖四版的存量不一致: 风机 CAPEX 选型门槛 0.5 kW vs F4 的 0.05 ——
  现以 **capexMinKw** 显式声明, 与选型门槛同源
- **断言 G (登记表声明自校验)**: 堵 v2.1 明示盲区「billedVia 可指向不存在的字段」。
  落地当天抓 5 条活体 —— 声明的是内部变量名 *CostDaily, 导出面是 *CostMax/Avg,
  与第六轮审计连错两轮的 dehumCostDaily 同一个坑。G2 防 CAPEX 条目重复认领

#### E. 🟡 skipClimate 删除 + 渲染解耦 + 误报驳回

- **skipClimate**: 全库零调用点的死参数, 已删 (为不存在的调用方准备的开关
  只会在被忘传时静默伤人)
- **渲染与计算解耦**: 渲染抛错此前伪装成「计算异常」并销毁算好的结果。
  现独立捕获 (「渲染异常, 计算结果有效」)。负对照双向实测: 修复前渲染故障
  使 13 条数值全灭为 0, 修复后零影响
- 驳回审计三条: NH₃ 阈值 / Ω 告警「六轮未动」为**第四轮已驳回误报的复发**
  (v1.9.3 起在位); 接触器进气节点不可选为记录在案的设计决策

### v2.1:能量守恒修复 + 守恒断言落地

源于**第三方第四轮审计**。13 条数值基线全部重算。

#### A. 🔴 车间显热能量守恒破口

v1.x 的能量分配是自洽的：泵/UV 电功率拆两份，一份进水（`Q_pumps`），一份进车间空气
（`Q_air_gain_kW`），相加恰为电功率。进空气那份在旧模型里进入**空气节点**能量方程。

v2.0 删除空气节点时，**挂在该节点上的产热项没有被重新安置**。`Q_air_gain_kW` 仍在计算、
仍在返回，但不进入任何一侧平衡 —— 既不在水侧，也不在暖通侧。**这部分能量从模型里凭空消失。**

同因还有第二处（本轮自查发现，审计未列）：`dehumHeatDest === 'indoor'` 时的除湿机冷凝热。
代码注释写着「排入车间的部分已在 T_room 迭代中计入」—— 而那个迭代在 v2.0 已被删除。

实测规模（三文鱼 1000 t 青岛设计冬）：丢失 **1,617 kWh/d**，达 HVAC 显热负荷 3,864 的 **41.8%**。

修法：`Q_hvac_sens = Q_envelope + Q_vent_sens − Q_room_internal`。

✅ 修复后成本变化的**符号随工况方向翻转**，这是正确性的旁证：

| 方向 | 工况 | Δ 工艺成本 |
|---|---|---|
| 制热（废热减轻供暖） | 青岛冬 / Bergen 冬 / 大菱鲆 | −0.22 / −0.15 / −0.14 |
| 制冷（废热加重制冷） | 海南冬 / 青岛年均 / 三亚年均 | +0.11 / +0.06 / +0.05 |

若为单向偏移，反而说明修错了方向。

#### B. 🟠 守恒类不变量断言落地（建议三轮未落地）

A 项那类缺陷**现有三类断言全都抓不到**：golden-master 抓不到（基线跟着一起变）、
有限性断言抓不到（数值完全正常）、口径一致断言也抓不到（不涉及同一量的两处换算）。
**只有守恒断言能抓。** 而同一版里出现两处同样成因的孤儿项，说明这不是偶发失误，
是**架构改动时缺少守恒护栏**的系统性风险。

三条已并入 `?selftest=1`，外部亦有同源脚本 `t20_energy_closure.js` 可挂 CI：

| 断言 | 式子 |
|---|---|
| A 设备产热分配完整性 | `Q_pumps + Q_uv + Q_air_gain×24 = 泵电功率 + UV电功率` |
| B 车间显热闭合 | `hvacSensible = Q_envelope + Q_vent_sens − Q_room_internal` |
| C 水侧净热闭合 | 含代谢/设备/补水/蒸发/除湿/接触器六项 |

修复前实测：A、C 通过，**B 在 56 个「工况×阶段」组合中 48 项失配**，
残差与 `Q_air_gain×24` 逐位相等 —— 诊断被精确坐实。

#### C. 🟠 自然冷却可见性回归

v2.0 新增 `fcUnavailReason` 时的**单行编辑失误**，把同行的 `freeCoolingSinkT: _fcSink,`
并进了行尾注释 —— 该字段自 v2.0 起不再导出，UI 第四档分支**永不触发**。
v1.9.3 修好的可见性问题因此回归，而本该替代它的 `fcUnavailReason` 做好了却**无消费点**。

本轮另发现第三层：整个状态段包在 `pumpDirection === 'cool'` 里，而 v2.0 最常见的原因
恰是 `not_cool`（接触器把热排光，水侧转制热）—— 即便前两层都修好，**最该解释的情形仍然不说话**。

现改为读枚举（四档 + 显式 default），并把 `not_cool` 提到方向判断之外。

#### D. 🟡 口径与参数四处

- **`totalMax` 补 `hvacCostDaily`** —— 此前只有 `totalAvg` 含它，而 `totalMax` 是面向用户的
  「小计·工艺运行成本」并导出下游。同为暖通电耗，除湿计入而显热不计，无理由
- **`dehumEnabled` 开关对成本完全无效却仍 gate 显示** —— 用户设「不配置」照付
  2,989 kWh/d（海南夏）却看不到卡片。实测开/关成本一字不差
- **`degasserExhaustIndoorFrac` 默认 0.2 → 0** —— 与气路拓扑 A5 对齐
- **`co2StripperGtoL` 引擎侧钳到 [1, 30]** —— 旧式只有下界，可被载入 JSON 绕过

#### E. 🟡 鱼种默认气候联动

v2.0 修正了自检基线的配对，但 **UI 默认仍是青岛且切鱼种不联动** ——
**测试修好了，产品还在发同一个陷阱**。新增 `DEFAULT_CLIMATE_BY_SPECIES` 并联动。

实测比电耗：罗非 16.21 → **13.48**，对虾 36.85 → **18.84**，鳜鱼 28.56 → 22.95。

#### F. 关于审计的两处不同判断

- 审计报「HVAC 潜热未计费，+0.42 元/kg」——**经复算不成立**。v2.0 已把除湿改为无条件的
  暖通职责并计入成本（海南夏实测 2,989 kWh/d）。真正的问题是**反向**的（见 D 项第二条）
- 审计报「三处对外基线未跟上重算」——**仅一处成立**。另两处在 `help-changelog-v192`
  章节内，是 v1.9.2 变更日志的**历史记录**，改它们等同于污染版本演进记录 ——
  正是 v2.0.0 版本号全局替换踩过的坑

#### G. 补齐三处漏项（第二批）

在审计「仍然开放」表逐条实测复核后补上，均为纯增项。

| 项 | 说明 |
|---|---|
| **车间暖通机组 CAPEX (AHU-1002)** | v2.0 引入车间显热能耗、v2.1 又修准了口径，却一直没有对应设备与投资（实测：CAPEX 17 条里有热泵、有除湿机，暖通 0 条）。按设计冬/夏显热峰值选型，合计 **85.5 kW / 24.4 W/m²**。⚠ 与热泵不重复：热泵调**水温**，本机组维持**室温** |
| **切换余量 `regimeMarginPct`** | 审计指出默认工况正坐在制度切换边界上（单项 +0.24~0.36 而合计 +2.49）。**v2.1 的守恒修复恰好移动了这个边界**（青岛冬 hvacSens 3,355 → 769）。实测 G:L=3 时余量仅 **3.9%**，默认阶段 1 仅 **0.5%** |
| **蛋分供气风机 (K-501)** | 此前只有支路泵没有风机，而泡沫分离的效率由**气量**决定。成鱼期 6.41 kW / 508 m³/h |

#### H. 🔴 登记完整性断言 —— 守恒断言抓不到的那一类

加蛋分风机时**连犯三个同类错误，现有断言一个都没抓到**：

| 错误 | 为何没抓到 |
|---|---|
| 风机并进 `totalPumpKw`（废热错误地进水侧） | 守恒断言 A 等式**两边同步放大**，残差为零 |
| 漏加 `elecCostAvgDaily` | 比电耗**单向低估**，该口径是手工枚举、无断言 |
| **气流未登记 `airPath`** | 风量平衡断言只校验**已登记项**之间是否自洽 |

第三条与 v2.0 的 `Q_air_gain` 孤儿项**完全同类**。结论：

> **守恒/平衡断言只能约束「已进入体系的量」，无法发现「压根没进体系的量」。**
> 要抓后者，必须从**设备侧反向枚举**。

新增 `POWER_REGISTRY` 登记表与两条断言：

- **D 气流登记完整性** —— 凡带 `airFlowM3h > 0` 的 spec，必须 (a) `airPath` 三字段齐全
  (b) 出现在 `collectAirStreams()` 输出中 (c) `spec.power` 与同名标量一致
- **E 电功率登记完整性** —— 凡 `/Kw$|Power$/` 且 > 0 的字段必须在 `POWER_REGISTRY`
  声明计费去向；且 `totalPumpKw` 必须等于其**声明成员之和**

四条负对照全部精确转红（用上面三个真实犯过的错 + 缺 `airPath` 构造）。其中
「风机并进 `totalPumpKw`」正是 t20 守恒断言**抓不到**而登记断言 E 抓住的那条。

> ⚠ 本断言仍有一层盲区：**登记表本身可以声明错**（如 `billedVia` 指向不存在的
> 成本字段）。堵它需校验该字段真实存在且非零 —— 未做。

#### J. 接触器净水汽计入水量平衡

v2.0 把接触器的**热**计入了水侧平衡，但**水的质量**一直没进 `V_makeup`。

**发现的两个问题：**

| 问题 | 说明 |
|---|---|
| **符号不对称** | 凝结水**减少**补水（已建模），但蒸发**不增加**补水。与 v2.0.1 修的 `totalKw_field` 同族：同一物理量的两个方向只守一边 |
| **纯水扣错了股** | 凝结水与除湿水是**纯水**，却去扣减**含盐的换水股** → 盐度漂低。已存在但从未显形（盐度不是状态变量，无处报警） |

**采用的简化 —— 拆两股水流：**

```
换水股  补水 V_exch (同盐度)  ←→  排污 V_exch (同盐度)
蒸发股  补淡水 E             ←→  蒸发失纯水 E
```

盐平衡因此**恒等成立** —— 不需要盐度状态变量、不需要「补水盐度」参数、不需要补盐 OPEX。
实测**换水股与排水量逐阶段完全相等**。

**实测规模（订正 HANDOFF v2.0.0 的记载）：**

| 工况 | 换水股 | 淡水股 | 余纯水 | 净水汽 | 稳态盐度 |
|---|---|---|---|---|---|
| 青岛设计冬 | 461.2 | **+29.6** | 0 | +29.6 | 30‰ |
| 青岛年均 | 461.2 | +11.7 | 0 | +12.5 | 30‰ |
| **海南设计夏** | 461.2 | 0 | **41.4** | −34.9 | **27.2‰**（−2.83） |
| 对虾三亚 | 126.8 | +4.6 | 0 | +5.7 | 15‰ |

> ⚠ HANDOFF v2.0.0 记的「584.6 kg/h，占补水 **6.0%**，**单向系统性偏差**」**三点都不准确**：
> 实测范围 **−10.8% ~ +10.7%，双向**。湿热气候下净结露，模型反而**高估**补水需求。
> 字段因此命名 `netWater` 而非 `evap`。

**断言 F（三条）**：F1 水量闭合（恒等式，转红即两股分离被破坏）／F2 盐平衡／
F3 热质同源（由 `mdot / W_in / W_out` **独立重推**净水汽量）。

> ⚠ F3 初版写成「潜热占比须在 [50%, 105%]」，用 Karimi 2020 的 **91%** 做区间 ——
> **判据错了**。那 91% 是**近等温工况**的实测值；青岛冬进气 −7°C 时占比 48.5%
> 是物理正确的，却被区间判红。**用文献单点值当断言区间，必须先确认该值的适用工况。**

#### K. 第五轮审计 — 风机热与登记表升级

**K1. 🔴 三台风机的热此前没有任何去处（P0）**

泵与 UV 有完整的电-热分配，但三台风机（**107.7 kW**）的电功率**既不在这个分配里，
也不在任何其它热流里**。注释写了「不走 `pumpHeatRatio` 进水」——对，但它也没走别的地方。

方向与 v2.0 的 `Q_air_gain` 完全一致：**模型付电费让风机运转，却让这些电产生的热
凭空消失，然后再付一次热泵的钱把水加热回来。**

修法：轴功（η=0.57）抬高接触器进气温度（青岛冬 −7 → **−3.5°C**），电机损失并入
`Q_room_internal`。⚠ 加热是**等湿过程**，绝对含湿量不变。

✅ 成本变化**符号随工况翻转**：制冷升（海南夏 +0.42），制热降（Bergen −0.23）。

**K2. 登记表升级为唯一真相源**

审计指出同一缺陷已在三层各出现一次：`Q_air_gain`（v2.0）→ 蛋分气流漏登记（v2.1 自查）
→ 风机热无去处（本轮）。成因是计费/热/资本三个去向分散在三处机制里。

`POWER_REGISTRY` 现声明四项：`path`（支持嵌套）/ `billedVia` / `heatVia` / `capexVia`。
断言改为**从登记表反向枚举**——旧 E 用正向扫描 + 命名规则，实测漏掉
`co2StripperSpec.blowerKw`(53.6 kW) 与 `bfBlowerSpec.power`(42.4 kW)。

> 「为『漏了蛋分风机』而建的断言，覆盖了蛋分风机，却覆盖不了另外两台大 4–5 倍的风机。」

新增 **A2**（全量电-热分配）与 **F4**（登记设备必须有 CAPEX 条目）。F4 落地后**立刻抓到**
蛋分风机「有电费无投资」——同一模式的第三次。

> ⚠ 实现过程中 A2 连报两次「已接入 0.00 kW」，两次都是真错（哨兵串被后续补丁注释污染；
> `const` 块作用域）。**若没有 A2，第二个错会完全静默**——数值正常、守恒 B/C 照常通过。

**K3. 气候联动只覆盖点击路径**

审计报「未修」结论不成立（字段叫 `DEFAULT_CLIMATE_BY_SPECIES`），但**撞到了真洞**：
联动只挂在按钮 click 处理器，`loadStagesFromDB()` 不调用，程序化路径全部绕过。已移入。

📌 意外收获：对虾基线 **−3.58 元/kg**——基线只覆盖年均字段，设备选型却用设计冬/夏，
三亚的对虾场一直按**青岛设计冬**选热泵。属长期不一致的修正。

**K4. 与审计的两处不同判断**

- **「HVAC 潜热未计费」不成立**（第二次报同一条）。海南夏潜热 4,473 → 除湿电 **2,613 kWh/d**。
  `hvacLatentKWhPerDay` 与 `dehumKWhPerDay` 同源，再计一次是重复
- **「买了 92 万除湿机一天不开」表述有误**。按设计夏峰值选型是正常做法。经产品方确认，
  除湿**按独立机组**处理

**K5. 断言 A3：账目完整 ≠ 接入正确**

A2 校验了「风机电功率 = 轴功 + 电机损失」，但**负对照没转红**——把轴功参数传 0
（热完全不接入），两边照样相等。补 **A3**（接触器进气温升必须与轴功自洽）后才咬人。

> **守恒断言容易只验证「账算得平」，而不验证「钱真的付到那个地方」。**

**K6. 收尾两项**

- **蛋分风机 1+1 备用** —— 补 `modulePerSpec` 规格后，三台风机备用政策一致
  （co2Blower / bfBlower / skimmerBlower 均 1+1）。与 v1.9.3 给脱气塔风机补规格时
  **完全同一处境**，当时的原注释逐字适用
- **v1.9.2 章节 G / I 两表加历史横幅** —— 数字（37.53 / 比电耗 9.5）**原样保留为历史记录**；
  但同章节末尾「请以本节为当前基线」这句**现在已指错**（当前基线是 v2.1），
  会让读者把 37.53 当成当前值，已加订正批注

> ⚠ **补丁顺序教训**：「meta 自基线生成」是构建期动作，正确位置是**链尾**。
> 第一次修复给了编号 48，结果新增基线补丁 51 又排到它后面，**同一问题立刻重现**。
> 现改为 **99**，让「链尾」由编号本身保证，而不是每次靠人记得。

#### I. 版本号与位号

- 版本号统一为 **v2.1**（此前 v2.1.0）。本批并入 v2.1，不另开 v2.2；
  v2.0.0 / v2.0.1 的历史叙述保持三段式原样
- 蛋分供气风机补位号 **K-501** —— 蛋分区已有 C-501 / P-501，同类的 K-101 / K-302
  也都有位号，唯独新加的风机没有。P&ID 注册表内带 `isBlower` 标志

### v2.0.1:缺陷修复 + 版本号自检

补丁版。**不改动任何计算模型** —— 13 条数值基线与 v2.0.0 逐位相同；自检 16 → **17 项**。

#### A. 模块化聚合 `totalKw_field` 产出 NaN

微滤机 RDF 是**流量定额**设备（走 `configureFlow()`，按 m³/h 选型），不返回
`installedKw`；而全场聚合循环对每个设备一律计算 `ms.installedKw × numModules`，
得 `NaN`。同一对象字面量的下一行 `totalFlow_field` 有守卫，本行没有 —— **不对称守卫**。

影响面已实测边界：16 个自检工况 × 全部阶段**只有这一条**非有限路径；`NaN` 经
`JSON.stringify` 静默变 `null`，下游四页面均未消费该字段 —— **线上无可见故障，属潜伏缺陷**。
修法与下一行同口径：算不出就明确给 `null`，不给 `NaN`。

#### B. 容积率 FAR 默认值 0.5 / 0.7 两个口径并存

v1.5 Bug 47 把 FAR 开放为参数时初值取 GB 50187 工业下限 0.7；「审计回合 7」认定水产
按农业 / 乡镇企业用地分类，改回实测主流 **0.5**，改了 UI 输入框与两处计算 fallback，
但漏了三处：

| 位置 | 旧 | 后果 |
|---|---|---|
| `collectState().farRatio` fallback | 0.7 | **唯一有实际后果**：存盘记 0.7 而导出用 0.5，再次载入把输入框刷成 0.7，**总占地静默缩小 29%** |
| `renderFarmSummary` 上方注释 | 0.7 | 误导维护者 |
| 占地估算的**用户可见文案** | 0.7 | 直接误导用户 |

三处全部归一到 0.5，与输入框 `value="0.5"` 同源。

#### C. 版本号:从人工待办改为自检项

页脚与「版本信息」面板此前是**硬编码字面量**，不读 `IRAS_META`（HANDOFF v2.0.0 §5
待办 #2 已标注）。而 index.html 里 `v2.0.0` 出现 9 次，**只有 5 处指向当前版本**，
其余是版本演进记录 —— v2.0.0 首版补丁全局替换过一次，把历史叙述一并污染。

现改为运行时从 `IRAS_META` 注入（单一事实源），HTML 字面量保留为 no-JS / SEO 回退；
新增自检项 `meta_version_consistency` 锁定四条：`<title>` / `og:title` /
`twitter:title` 三处 SEO 静态串含当前版本号，页脚与面板注入结果与 `IRAS_META` 一致。

**该断言已用负对照验证会失败**（改一处 SEO 串 → `og:title 不含 v2.0.1`；删一个注入锚点
→ `#footerVersion 元素缺失`），不是恒真判据。

> ⚠ 本版**未**触碰 HANDOFF §5 列出的 v2.1 候选项（接触器蒸发进水量平衡、凝结水对盐度
> 影响、MBBR 流化下限、蛋分供气动力等）—— 那些都要动模型，不属补丁版范围。

### v2.0.0:热平衡架构重构 + 气液接触器 + 规范对标

自 v1.2 引入双节点热平衡以来**最大的一次模型改动**。热平衡代码**净减少 65 行**；
13 条数值基线全部重算；自检 14 → **16 项**。

#### 车间温度：从因变量改为输入

v1.2–v1.9.3 把 `T_room` 作为空气节点能量方程的**解**，自由漂浮、无任何调节系统。
后果：热带鱼种算出车间 **9–14 °C / RH 99%**。

而实测与规范一致指向「空气 ≥ 水温」：

| 来源 | 车间气温 | 水温 |
|---|---|---|
| 摩洛哥 Amghass 站实测（AQUAIR 数据集 84 天） | 16.0 均值 | 鳟鱼 12–16 |
| 大连河豚车间 CFD + 实测 | 22–23 | 17 |
| ASHRAE HVAC Applications Ch.6 / CJJ 122-2017 | 水温 +1~2 °C，RH 50–70% | — |
| **v1.x 本工具（热带鱼种）** | **9–14** | **25–30** |

**方向反了 15–20 K。** v2.0 改为 `T_room = T_水温`（暖通维持），`roomRH_set` 默认 65% 可调。
由此删除 Picard 迭代、闭式解、钳位与「钳位时静默丢弃能量」缺陷；池面显热 `Q_pool_loss ≡ 0`。

> 由此消失的长期争议：水面传热系数取 5 还是 15、太阳辐射要不要建模、钳位缺口去哪
> —— 温差为零后**全部无关**。

#### 气液接触器 —— 本版核心新增

v1.x 中脱气塔与 MBBR 曝气的气水直接接触量 **96,287 m³/h**（机械新风的 **4.2 倍**）
**完全不在热平衡账内**。

出口按**水温下饱和**计算，**无需标定系数**，两个独立领域实测支撑：

- Karimi 等 2020（Wageningen，8 组 GLR）：出口 RH 实测 **98–100%**，与进水温差平均 0.77 °C
- Talati & Stenstrom 1990（ASCE）：鼓风曝气 hf = 1，尾气实测现场研究确认

**Δh 可为负** —— 湿热空气遇冷水在填料上结露放热：

| 三文鱼 1000 t 年均 | 接触器净排热 kWh/d | 比电耗 |
|---|---|---|
| Bergen | +16,405 | 10.46 |
| 青岛 | +9,460 | 11.54 |
| 上海 | +1,123 | 12.45 |
| Doha | −11,527 | 15.37 |
| **三亚** | **−18,470** | **15.96** |
| 三亚（设计夏） | **−53,482** | 24.73 |

**脱气塔在热带是凝结加热器而非冷却塔。** 这是 Atlantic Sapphire（佛州 Homestead）、
宁波象山 Nordic Aqua 一类项目的核心成本，行业内普遍未量化。

#### 自然冷却与接触器：抢同一个资源，不叠加

v2.0 接入接触器后，**自然冷却在冷天基本失效** —— 而那正是它原本最有价值的场景。
两者用的是**同一个物理资源：室外冷空气**，而**接触器是必开的**（脱 CO₂ 是刚需）。

| 工况（三文鱼 1000 t 青岛） | 水侧方向 | FC 贡献 g | FC 开→关 比电耗 |
|---|---|---|---|
| 设计冬 | **heat** | 0.00 | 12.57 → 12.57，**Δ 0** |
| 年均 | cool | 0.13 | 11.54 → 11.59，Δ 0.049 |
| 设计夏 | cool | 0.00（室外 32 °C 过热） | Δ 0 |

> **⚠ v1.9.2 的自然冷却效益结论已订正。** 「寒冷气候热泵费 1.68 → 0.29 元/kg」
> 是在**接触器缺失**时算出的 —— 那部分「省下的电」其实早已被脱气塔省掉，**两者不叠加**。

UI 保留开关：年均仍有 13% 贡献（年均才是能耗口径）、可作对照基准、
且真实工程可冬季降 G:L 把负荷让给自然冷却（模型暂未建）。

新增 `fcUnavailReason` 区分四种不可用：`off` / **`not_cool`（水侧需制热，v2.0 主因）**
/ `uneconomic` / `no_dt`。

#### 暖通能耗计入

三部分现已全部进电耗：水侧热泵（`thermalCostDaily`）+ 除湿机（`dehumCostDaily`，v1.9.2 即有）
+ **车间围护与通风显热（`hvacCostDaily`，本版新增）**。

`T_room = T_w` 后车间与室外温差恒为「水温 − 气温」：正 = 失热需加热，负 = 得热需制冷。
**加热与制冷 COP 不同，分别取温度修正后的 COP**，不可对绝对值统一折算。
该项在寒冷地区与水侧热泵同量级（Bergen 设计冬 3,355 vs 2,847 kWh/d）。

#### 脱气塔对标教科书

`co2StripperGtoL` **3 → 5**，新增 `co2PackingHeight_m` **1.5 m**。
依据 Timmons Ch.10 p.244–245：CO₂ 汽提 G/L 为 **5:1–20:1**（常规曝气才 <3:1，
增氧 0.003:1–0.05:1，三个用途差三个数量级）；填料高度限于 1.0–1.5 m。
Karimi 2020 实测 GLR > 5 无额外增益、Summerfelt 2003 最优 5.1 —— **四个来源一致**。

比功率 3.47 → **5.79 kW/1000 m³/h**，进入 Summerfelt 2009 的 5–15 区间，
顺带解决 v1.9.2 审计 P4 遗留。

> **风压保持 2.5 kPa。** 原注释建议上调至 4.3–8.0 kPa 是把**曝气风机档位误套到脱气塔**
> —— 曝气克服静水压（约 10 kPa/m 水柱），脱气只克服填料流动阻力
> （化工准则 0.2–0.4 kPa/m，液泛点才 1.3 kPa/m）。

#### 鱼种参数库水质自洽性修正

新增 `t17` 断言发现**三处参数组合物理不可行**。共同点：`density` 或耗氧系数在某版本被调高，
而 `turnover` 未同步 —— **每次改动单独看都对，组合起来把参数推到物理不可行区**：

| 鱼种/阶段 | 原状态 | turnover |
|---|---|---|
| 鳗鲡 养成期 | **出池 DO = 0.00 mg/L**（完全耗尽），CO₂ 超阈值 44% | 2.5 → 5.0 |
| 鳗鲡 黑仔期 | 出池 DO 0.69（阈值 5.09） | 3.0 → 4.5 |
| 罗非 苗种期 | 出池 DO 3.20（阈值 4.70） | 6.0 → 8.0 |
| 大菱鲆 中鱼期 | 池内 CO₂ 15.2（阈值 15） | 2.5 → 3.0 |

以鳗鲡为例：v1.9.2 把养成期密度由 40 上调至 120 kg/m³（修正 CAPEX 失真），
同版新增 TSS 附加耗氧项 —— 高密度下该项占总耗氧 **67%**，两者叠加使出池溶氧归零。

> **golden-master 基线对此无能为力** —— 它们从未 FAIL 过，因为基线记录的正是错误状态本身。
> 这类缺陷需要**面向工程约束**的断言。

#### 基线工况按【鱼种 × 地区】实际配对重设

v1.x 起有 **7 条基线没有 `climate` 字段**，全部跑在默认地区**青岛** ——
罗非（28 °C）/ 石斑（27 °C）/ 对虾（30 °C）在 −7 °C 过冬，**工程上不存在**。
接触器项接入后气候成为主导项，温水鱼种比电耗被推到 15–30 kWh/kg
（行业实测上限为 Atlantic Sapphire 丹麦的 13.6）。

#### 成本基线（13 条数值工况）

> ⚠ 全部由 `gen_base.js` 走真实 `recomputeAll()` 生成，**不要手抄**。

| 工况 | 地区/工况 | 工艺运行成本<br>元/kg | 企业全成本<br>元/kg | 比电耗<br>kWh/kg·yr | 热泵电费<br>元/kg |
|---|---|---|---|---|---|
| salmon_qingdao_annual | 青岛 年均 | 39.77 | 44.89 | 11.54 | 1.06 |
| salmon_qingdao_w | 青岛 设计冬 | 39.31 | 44.41 | 12.57 | 1.30 |
| salmon_hainan_w | 海南 设计冬 | 39.36 | 44.46 | 12.44 | 1.53 |
| salmon_hainan_s | 海南 设计夏 | 43.46 | 48.81 | 18.43 | 4.51 |
| salmon_bergen_w | Bergen 设计冬 | 37.86 | 42.87 | 10.46 | 0.35 |
| **salmon_sanya_annual** | **三亚 年均（新增）** | **42.13** | **47.40** | **15.96** | **3.30** |
| tilapia_1000 | 上海 年均 | 30.94 | 35.60 | 9.77 | 0.85 |
| turbot_500 | 青岛 年均 | 55.84 | 66.01 | 16.31 | 1.67 |
| grouper_500 | 三亚 年均 | 44.54 | 54.24 | 14.14 | 1.93 |
| bass_300 | 上海 年均 | 46.26 | 53.21 | 12.80 | 1.11 |
| eel_200 | 上海 年均 | 60.96 | 116.92 | 12.83 | 1.07 |
| mandarin_200 | 上海 年均 | 58.77 | 70.62 | 16.51 | 1.47 |
| shrimp_100 | 三亚 年均 | 53.23 | 70.47 | 19.56 | 2.75 |

**新增 `salmon_sanya_annual`** —— 冷水鱼种 × 热带气候，当前行业热点，
也是接触器项最关键的场景，此前无任何基线覆盖。

#### 比电耗外部对标

| 来源 | kWh/kg | 性质 |
|---|---|---|
| Atlantic Sapphire 丹麦 2020 年报 | 13.6 | **实际运行**（12.1 GWh ÷ 1,022 t） |
| **iRAS 青岛年均** | **11.54** | 本工具 |
| **iRAS Bergen 年均** | **10.46** | 本工具 |
| Ayuso-Virgili 等 2023（HVL Bergen） | 9.59 | 挪威独立仿真 |
| Song 等 2019 LCA | 8.4 | 中国 RAS 三文鱼现场实测 |
| 系统性综述（2010–2024，25 组数据） | 商业均值 6.8 | 全域 0.77–29.4 |

iRAS Bergen 年均与挪威独立仿真差 **9%**。

#### 新增三项断言（t15–t17）

| 测试 | 内容 |
|---|---|
| **t15 风量平衡** | A：设备层**独立扫描**出的动风设备必须已在气路拓扑登记（判据只看字段名形态，不读登记表，否则退化成「把实现抄一遍」）；B：接触水气流合计必须等于热平衡实际计入量 |
| **t16 接触器物理锚** | 复现 Karimi 2020 实测。**有界检验**而非精确匹配：出口状态 / 潜热占比 >80% / **Δh 符号**（湿热气候养冷水鱼必须为负）/ 量级 0.9–1.5× |
| **t17 鱼种水质自洽** | 硬约束（出池 DO、池内 CO₂/TAN/TSS）+ 余量哨兵（<10% 告警）+ 负荷指标（饲料/流量比） |

#### 经核实【无需修改】的七项

调查中曾判定有问题、经查规范与实测后确认模型正确：

| 曾判定 | 核实依据 |
|---|---|
| DO 判据口径错误 | Timmons Tab 3.1：鲑科**出水** 6–8 / 罗非 4–6 mg/L，本工具在区间内 |
| turnover 偏高 3 倍 | **DB 21/T 2350-2024 §9.3 要求溶氧 8–12 mg/L**，turnover 3.0 是满足规范的**最低值** |
| 耗氧强度偏高 | MO₂ 3.15 mg/(kg·min) @15 °C，文献 2–4 中部 |
| 主泵扬程未生效 | 参数正确（7 m + 氧锥 3 m = 10 m），反算 9.5 m 吻合 |
| 比电耗偏高 | 低于 Atlantic Sapphire 实测 13.6，高于挪威仿真 9.59 |
| 脱气塔风压偏低 | 与 G:L=5 组合后比功率 5.79，进入文献区间 |
| 围护 U 值缺失 | **早已完整**：U_墙/顶/地/窗 + 4 档保温预设 + 热回收 + 窗墙比 |

> **教训：先查规范与实测，再判断模型。** 本轮多次因先用论文反推、或跨温度跨鱼种直接比较
> 而误判 ——「turnover 偏高」绕了七八轮，而辽宁省标准一条即可定案。

#### 已知限制

> ⚠ **v2.1 订正**：本节记录的是 **v2.0.0 当时**的状态，其中**四条已在 v2.1 解决或推翻**，
> 原文保留作历史记录，当前状态见上方 v2.1 章节：
> - **① 接触器蒸发未计入水量平衡** → **已实现**（补水拆换水股/蒸发股，盐平衡恒等成立）。
>   且原文三个数字均不准确：实测 **−10.8% ~ +10.7%、双向**（非「6.0%、单向」），
>   接触器净水汽约为池面蒸发 **50 倍**（非 29.5 倍）
> - **② 凝结水对盐度的影响未建模** → **已给出诊断**（海南设计夏稳态盐度 30 → 27.2‰）
> - **暖通侧负荷标注界外** → **已推翻**：v2.0 起计入能耗，v2.1 修正口径并补 AHU-1002
> - **蛋分有支路泵而无供气动力项** → **已实现**（K-501，6.41 kW / 508 m³/h）
<!-- IRAS_V21_LIMITS_NOTE_RM -->

**① 接触器蒸发未计入水量平衡。** 热量已完整计入（Δh 含潜热，t16 验证潜热占 86%），
但水的**质量**尚未进入 `V_makeup`。实测三文鱼青岛设计冬：接触器蒸发
**584.6 kg/h（14.03 m³/d）**，是池面蒸发 19.8 kg/h 的 **29.5 倍**，占补水量 **6.0%**。
补水量、补水加热、补水费与排污量均系统性低估约 6%。**属单向偏差，列入 v2.1。**

**② 凝结水对盐度的影响未建模。** 湿热气候 Δh 为负时净产水，三亚设计夏约 **33 t/d**。
淡水场是白得的补水；**海水场会被持续稀释**，影响换水策略与盐度控制。

**③ 暖通侧未含太阳辐射与人员负荷。** 围护体系本身完整（U 值四项可调 + 4 档保温预设
+ 通风热回收 + 地板对地温单独算），但太阳辐射与采光带得热未建模 ——
大连 RAS 车间 CFD+实测显示屋顶采光带可把墙温推到 29 °C（同期水温 17 °C），
故**高日照、设采光带的车间夏季制冷负荷偏保守**。

其它遗留：`contactorRecovery` 默认 0；MBBR 流化下限与 `bfTankDepth` 参数缺失；
石斑三阶段 DO 余量仅 +2%/+3%/+10%；蛋分有支路泵而无供气动力项；
单池面积 DB 21/T 2350 规定 5–30 m²，本工具每阶段一个池、池数由用户设置。

### v1.0–v1.3:工艺设计与工程化
- **多阶段养殖规模反推**(尾数流守恒 + Little's Law)
- **精确循环方程水质稳态仿真**(TAN / TSS / DOM / NO₃ / DO / CO₂)
- **主流串联 + 旁路并联**混合建模(蛋分 / 反硝化 / AOP)
- **盐度修正**(DO 饱和度 + 硝化效率 + 海水臭氧溴酸盐警告)
- **设备规格与选型**(RDF / BF / UV / CO₂ 脱气 / 蛋分 / AOP / 反硝化 / 增氧 / 泵)
- **多模块并联 + N+1 备用**(v1.3 新增)
- **P&ID 工艺图自动生成**(v1.3.x 新增)
- **设备清单导出**(v1.3.x 新增)

### v1.2:全局气候 + 双节点稳态热平衡
- **温带 / 亚热带 / 北欧 / 中国南方** 4 工况一键切换
- **车间空气-水体双节点联立求解**(车间散热 + 鱼代谢热 + 板换回收)
- **挪威 / 海南 / 青岛**等典型场景热泵装机自动估算

### v1.4:投资财务评价
- **NPV / IRR / 投资回收期 / 盈亏平衡点**完整计算
- **8 张标准财务表**(收入 / 成本 / 利润 / 现金流 / 还款 / 敏感性等)
- **5 因子敏感性分析**(售价 / CAPEX / 产量 / 饲料 / 电费)
- **Excel 一键导出**(SheetJS,7 个工作表)

### v1.5:可研报告生成器
- **16 章扩展版可研报告**(发改委政府立项编制规范)
- **20 字段项目元数据**(项目身份 / 业主 / 用地 / 编制单位)
- **3 张嵌入图**(工艺流程示意图 / 累计现金流 / Tornado 敏感性)
- **8 鱼种条件分支**(三文鱼 / 大菱鲆 / 石斑 / 加州鲈 / 鳜 / 罗非 / 鳗 / 对虾)
- **.docx 文档输出**(浏览器侧 docx.js 生成)

### v1.6:站级设备 + UI 重构
- **液氧储罐站级化** — 修复跨阶段重复计 CAPEX(千吨级项目精度提升约 3%),P&ID / 设备清单 / 报告全部同步
- **UI 整体重构** — 可折叠分区(`<details>`)+ sticky 双行工具栏 + 2×2 KPI 卡片 + 配色克制化
- **8 项工程逻辑精化** + **6 处手册公式订正**

### v1.7:氧锥拓扑 + K-101 + 系统排水重构 + 全面审计修复

- **氧锥工艺拓扑 mainline / bypass 真实建模** — 新增 5 个工艺字段,P&ID 自动重画 bypass 三件套(A-601 + P-602 + M-601 混合点)。**1000 t 大西洋三文鱼实测节能 30 kW + CAPEX 节省 612 万元**
- **K-101 鱼池增氧鼓风机(air 模式专属)** — air 模式补齐,覆盖罗非 / 鳗 / 对虾等温水鱼种
- **系统排水重构** — 删除"RDF 反冲(%)"输入,新增"系统排水"卡片(流量按 `V_total × exchangeDaily` 算,质量守恒)
- **全面审计修复 27 项** — 含 #25 灾难性 bug (`buildContext()` return 漏 `workflow`,致 55 处条件分支失效,潜伏 1+ 年)

### v1.9:碳酸盐平衡 + O₂ 物理校准 + CO₂ 公式修正

中文版直接从 v1.7 跳到 v1.9, 与英文版同步。本次升级把工具从"工程算成本工具"推进到"工程算成本 + 水化学合规工具"。

- **碳酸盐平衡求解 (carbonate.js, 226 行 独立模块)** — 完整水化学物种平衡, 平衡常数 verbatim 移植自 **PyCO2SYS 1.8.3** (国际标准):K₁/K₂ (Millero 2010, 盐度 0-50) / K_B / K_S / K_a(NH₃) (Clegg & Whitfield 1995) / K_sp 方解石 (Mucci 1983)。vs PyCO2SYS 验证 max|ΔpH| = **1×10⁻⁵** (实验室级精度)
- **两种 pH 控制策略** — 工艺面板新增"pH 控制"下拉:
  - `持碱度` (默认):设碱度目标,pH 由平衡涌现
  - `持目标 pH`:设目标 pH,工具反推所需碱度
- **沿程水质 UI** — 鱼池节点新增 **pH / NH₃-N (μg/L) / β 缓冲强度 / 碱度需** 4 行显示,鱼种敏感性着色
- **O₂ Q10 参考温度对齐** (validation memo F1) — 把 Q10 参考温度从硬编码 20°C 改为物种 `metabRefTemp`,与热模型一致。同时**数学等价重写** o2BaseFactor (8 鱼种):salmon 0.32 → 0.2263, turbot 0.28 → 0.1980, tilapia 0.16 → 0.2786 等。数值结果不变,但语义更清晰(新值直接是 tempOpt = metabRefTemp 时的 O₂:feed 比)
- **CO₂ 公式修正** (validation memo F2) — v1.7 之前 `co2Daily` 只算呼吸,漏算硝化产 CO₂。v1.9 加入 NH₄⁺ + 2HCO₃⁻ + 2O₂ → NO₃⁻ + 2CO₂ 反应贡献(**6.286 g CO₂/g TAN-N**),1000 t 三文鱼成鱼期 co2Daily 从 700 → 1221 kg/d(+74%)
- **finance-core.js 零利率 bug 修复** — 等额本息公式在 r=0 时除零产生 NaN,v1.9 修复(影响政策性贴息贷款工况)
- **技术手册升级至 124 页 v1.9 完整版**(新增 §4.5 碳酸盐平衡章节 + v1.9 演进章节)

> **重要**:v1.9 升级**不改变** 1000 t 三文鱼综合成本基准(当时为 **36.70 元/kg**)。o2BaseFactor 是数学等价重写,数值结果与 v1.7 完全一致。
>
> *(该基准已被 v1.9.1 / v1.9.2 的后续修正取代 —— 当前值见下方「9 工况实测数据」。此处保留 v1.9 当时的记录。)*

完整发布说明见 [v1.9 Release Notes](https://github.com/chengsunmail/iras/releases/tag/v1.9.0)

### v1.9.1:液氧口径修正 + 蒸发系数量纲修正

与英文版 v2.0 同步的两处物理/口径修正。**不改变设备选型结果**,仅修正消耗品采购量与蒸发潜热。

- **液氧消耗口径修正** — 旧版用含选型余量的 `o2Demand`(含 `peakFactor` × `sAerator`)计算液氧 OPEX 与基准值。液氧是**按量计费的消耗品**,只有吸收效率应放大采购量,容量系数不应参与。新增并行口径 `o2ConsumptionDaily` / `o2ConsumptionDailyAvg`,`o2Demand` 原样保留给设备选型
  - 液氧成本:**高估 65% → 修正**(旧/新 = 1.5 × 1.10 = 1.65)
  - 鲑鱼成鱼液氧基准值:0.44 → **0.266** kg O₂/kg 饲料(文献 0.25–0.30)
  - 液氧年耗(1000 t/yr, FCR 1.2):527 t → **319 t**
  - 液氧储罐(7 天储备):10.1 t → **6.1 t**
  - 验收区间由 0.25–0.95 收紧为 **0.20–0.45**(旧区间是为迁就被膨胀数值而放宽的)
- **蒸发系数 β 量纲修正** — 旧版注释声称 β 取自 ASHRAE Applications Ch.6 的 0.045–0.075 m/s,**这是错误出处**。ASHRAE / Carrier 的池面蒸发系数是定义在**蒸气压差**上的英制系数 [lb/(h·ft²·inHg)],不能直接当作 ΔW × ρ_air 形式的 β [m/s] 使用,否则高估约一个数量级
  - 改用 Lewis 传热传质类比导出:`β = h_c / (ρ·c_p·Le^(2/3))`,β ≈ **0.0035–0.010 m/s**(旧 0.045–0.15)
  - 交叉验证:把 ASHRAE 的 0.089 换算到湿度比基准,同样得到 β ≈ 0.005 m/s
  - 实测效果:蒸发率降 2–2.4 倍(非 13 倍,因 `W_room` 自洽求解存在自补偿);**车间湿度饱和 (RH=99%) 告警消失**

> **回归验证**:`o2PeakSupplyKgH` 与 `o2Demand` 计算式**逐字未变** — 本次改的是"买多少氧",不是"配多大设备",氧锥流量与设备规格与 v1.9 完全一致。

依据详见 `VALIDATION.md` §2(蒸发系数)与 §3(液氧口径)。

### v1.9.2 (第一批):通风量口径修正 + 机械除湿机

热平衡模型的通风量口径修正,并新增机械除湿机建模。**不改变设备选型逻辑**。

- **通风量口径修正** — 旧版通风量 = `ACH × 车间体积`,而车间面积由 `1.6 × 水面积` 自动估算、水面积 = 水体/池深。结果:**养殖密度降低 → 水体↑ → 车间↑ → 通风↑ → 湿热气候下凝结放热↑ → 制冷装机暴涨**,但鱼的数量没变,新风需求本不该变
  - 新口径:`新风量 = max(ACH × 车间体积, 鱼载荷 CO₂ 稀释需求)`
  - CO₂ 需求依据文献 500 g CO₂/kg 饲料(大西洋鲑 409 Terjesen 2013 / smolt 550 Vinci 2004 / 鳗鲡 536 Heinsbroek 1990)
  - 新增可调参数:**车间 CO₂ 限值**(默认 1500 ppm)、**脱气塔尾气入室比例**(默认 0.2)
  - **ACH 默认值 1.0 有了标准依据** — 泳池馆新风标准 0.48 CFM/ft² 湿面积换算,本模型几何下约 0.9–1.5 ACH
  - **除湿不计入新风**:湿热气候通风不能除湿(室外空气比车间更湿),干冷气候靠通风除湿仅需约 0.14 ACH — 两种情况下除湿都不是通风量的决定项
- **新增机械除湿机建模** — 湿热气候养冷水鱼时通常必配(室外更湿,通风无法除湿)
  - 开启后车间湿度被控制在目标 RH,水面凝结减少 → 制冷负荷下降
  - 计算除湿量(kg/d)、除湿机电耗(kWh/d,按 SMER)、冷凝水回收(可抵扣补水)
  - 参数:开关 / 目标 RH(默认 70%)/ SMER(默认 2.5 kg·kWh⁻¹)/ 冷凝水回收
  - 海南夏工况实测:开除湿机后车间 RH 由 92% 降至 70%,凝结放热 2152 → 1222 kWh/d,除湿机电耗 1365 kWh/d,冷凝水回收 3.4 t/d

> **工程意义**:总电耗相近,但车间湿度从 92% 降到 70% — 配除湿机的真实理由不是省电,而是避免霉菌、设备腐蚀与电气故障。

### v1.9.2 (第二批):泵功率口径 + 耗氧-产热绑定 + 成本模型两层化

在第一批基础上继续修正物理口径,并把成本模型从「工艺运行成本」扩到「企业全成本」。**设备选型逻辑仍不变**(7 项 × 4 阶段回归逐位一致)。

**物理口径修正**

- **泵功率口径 —— 轴功率 vs 电功率** — `shaftKw = 9.81QH/(3600η)` 是**轴功率**,不含电机效率。旧实现 (1) 把它直接当电耗计费,漏算电机损失;(2) 用 `pumpHeatRatio = 0.85` 把 15% 轴功率送进车间空气 —— 那是把「电机效率 85%」错记在轴功率上,而电机损失根本不在这个数里
  - 新增 `motorEta`(默认 0.95),电耗 = 轴功率 / motorEta
  - `pumpHeatRatio` 0.85 → **0.97**;空气得热 = 轴功率 × (1/motorEta − pumpHeatRatio)
  - **车间得热原高估约 2.8 倍** → 导致 T_room 顶穿物理钳位、RH 被人为压低到除湿机永不触发
- **T_room 钳位不再静默截断** — 钳位触发时报出未闭合能量缺口。修正泵口径后钳位触发率由 30% 降至 8%
- **代谢产热改由耗氧导出** — 旧式 Schrama 系数 106 kJ/(d·kg^0.85) 源自**饱食投喂**实测且公式无投饵率变量,套商业投饵率高估约 3 倍(1.11 W/kg vs 维持代谢基准 0.11)。改为 `池内耗氧 × 13.6 kJ/g O₂`(Brett & Groves 1979),**11030 → 4053 kWh/d**
  - 同一代谢过程同时决定耗氧与产热,由氧热当量刚性绑定,不再各取各的文献值
- **池内耗氧新增 TSS 依赖项** — 悬浮固体上附着的异养菌在**池内**直接耗氧,同样要靠氧锥供给
  - **固体管理首次与液氧账单挂钩**:微滤机去除率 90% → 40%,池内 TSS 3.0 → 5.0,液氧年耗 **334 → 481 t (+44%)**
- **总溶解气压 (TGP) 成为模型量** — `TGP = 100 + (饱和度 − 1) × 20.95`,阈值鱼苗 110% / 其他 115%,接入 DO 告警

**默认值修正**

| 参数 | 原 | 新 | 理由 |
|---|---|---|---|
| `mainPumpHead_m` | 12 | **7** | 12 是「多级跌落」档,与工具自身提示的「重力回流 5-8」矛盾 |
| `mainPumpEta` | 68 | **75** | 大流量低扬程工况选轴流/混流泵 |
| 支路泵扬程 | 10 | **7** | 支路流量小、管路短,不应高于主泵 |
| `o2SatTarget` | 各鱼种 110-165 | **统一 165** | DO 包线体检发现 5 个阶段击穿、4 个临界 |
| 苗种期饱和度微调 | +5 | **−20** | 原方向与气泡病风险相反 —— 鱼苗最敏感却配得最高 |

比电耗 17.9 → **12.0 kWh/kg**。

**审计发现并修复的 Bug**

- 除湿机电耗未计入 OPEX —— 开除湿机反而降成本,物理上不成立
- 除湿机无 CAPEX、不进设备清单
- `dehumCondRecovery` 断链 —— 有默认值有用途,但无 UI / 无载入 / 无监听
- v1.9.2 第一批的全部新输出(除湿量/电耗/排热/等效 ACH)算了但没有界面出口
- 一处与 v1.9.1 修正自相矛盾的陈旧注释

**DO 击穿告警改为按约束诊断**

旧文案建议「提高循环倍数或 DO 目标饱和度」。统一到 165% 后,「提高饱和度」已成为**危险建议**(会撞进刚规避的气泡病风险)。改为逐条计算四条出路的当前值、所需值与卡点:降密度 / 提 turnover(>6 次/h 标注为工程上做不到)/ 提饱和度(超 TGP 阈值则标红劝阻)/ 改善固体管理。

**成本模型两层化**

```
工艺运行成本 = 饲料 + 工艺电耗 + 工艺消耗品 + 维护 + 保险 + 补水 + 排污 + 折旧
企业全成本   = 工艺运行成本 + 苗种 + 人工 + 管理费
```

新增 7 个成本项、11 个可调参数(维护费率与基数 / 保险费率 / 管理费率 / 劳动生产率 / 人均年成本 / 最小班组 / 补水淡海水单价 / 排污单价 / 苗种单价)。

**分两层输出**的理由:工艺侧每一项都能追到物理量与公式;企业侧本质是经济估算。混进一个总数会拉低整个模型的可追溯性。

> 与 Nordic Aqua(宁波象山)2025 年报对标发现,旧口径比商业实绩低约 38%,差额几乎全在「其他 OPEX」—— 缺的是维护、保险、补水、排污这批项目,不是物理算错了。详见技术手册 21.5。

### v1.9.2 (第三批):第三方审计修复 + 自然冷却 + 自检入口

v1.9.2 主体完成后接受了一轮第三方代码审计，修复 **8 项缺陷**，并新增自然冷却路径与自检入口。

**修复的缺陷**

| 级别 | 问题 | 后果 |
|---|---|---|
| 严重 | `o2FishFactor` 回写污染 | UI 把含 TSS 附加的值回写进输入框，被当作"手填的纯生理值"读回后再叠一次 —— **每操作一次 +8%，可无限累积**。改用 `placeholder`，自动值与手填值在数据结构上彻底分开 |
| 严重 | 生物滤池节点氮不守恒 | 同一节点 TAN 只去除 η、NO₃ 却按 100% 增加。低 η 时失控：反硝化进水 NO₃ 算出 115.7（应 4.25），进而 **NaHCO₃ 投加量算成 0** —— 模型给出"不用加碱"的危险结论 |
| 重要 | 热泵按**电功率**计价 | 2000 元/kW 是"元/kW 制冷量"口径，旧实现传电功率，CAPEX 缩到约 1/COP |
| 重要 | CO₂ 产量两个口径 | 通风用文献常数 500，模型自算 671–1084 g/kg 饲料，**差 34%–117%** |
| 重要 | 发散被静默钳制 | 无去除路径时浓度本应发散，防除零钳制却给出"看起来精确"的有限值（TAN 339.89 mg/L）。新增显式告警 |
| 重要 | BF 去除率与接触时间无校核 | 可填 η=95% 却配空床停留几十秒的 BF。新增 EBCT 判据 |
| 一般 | 空场 NaN 污染 | 清空产量输入框（必然出现的中间态）→ 0/0 → NaN 沿成本链路传播，全场汇总与财务评价全显示 NaN 且无提示 |
| 一般 | 池内耗氧 TSS 依赖式**漏了截距** | 实测两点定线只取了斜率、丢了截距，等于假设"TSS=基准时池内除鱼外无任何耗氧"。补 0.074 后两个实测锚点（TSS 3.0→0.30、TSS 11→0.70）都能对上，默认工况池内耗氧 0.246 → 0.320，与工程设计经验值 0.35 同量级 |
| 安全 | 方案 JSON 导入 DOM-XSS | `stage.name` 等来自文件的字符串直接拼进 `innerHTML`。而"设计院之间互发方案 JSON"正是预期用法 |

**新增:自然冷却 (free cooling)**

旧模型在**任何**制冷工况下都走机械压缩制冷、COP 封顶 3.45。但冬季室外 −7°C 时完全可以用干冷器/板换直接排热，压缩机不启动，能效比可达 15–30。

| 工况 | 气温 | 自然冷却 | 热泵电耗 |
|---|---|---|---|
| 青岛冬 | −7°C | ✅ | 634 kWh/d |
| Bergen 冬 | 0°C | ✅ | 646 |
| 海南冬 | 17°C | 否 | 4272 |
| 海南夏 | 32°C | 否 | 5118 |

寒冷气候热泵费 1.68 → **0.29 元/kg**，比电耗 12.2 → 10.2。**跨气候成本差由 0.99 拉大到 1.78 元/kg** —— 寒冷气候本就该因免费制冷而占优，旧模型把这个优势完全抹掉了。

**参数取向调整:反硝化旁路 10% → 2%**

旧默认使稳态 NO₃-N 仅 4.2 mg/L(饮用水级别),且对应 1015 m³/h 的反硝化反应器(主循环流量的十分之一)。改为 2% 后 NO₃-N 20.2 mg/L、反应器 203 m³/h,落在「配了反硝化的 RAS」合理区间(20–60)。

> **⚠ 关于换水率**:iRAS 各鱼种的换水率折算成产业口径「单位投饵补水量」后为 **0.055–0.216 m³/kg 饲料**,
> 全部压在产业下沿(极集约 RAS 0.1–0.3、常规商业 0.3–1.0、半流水 >3),鳗鲡/罗非/鲑鱼三种甚至低于极集约下限。
> 这是**刻意的极致节水取向**,不是缺陷。
> 代价是硝酸盐排不出去 —— 把 `denitriFlowPct` 调到 0 时算出的 NO₃-N 414 mg/L **是真实后果不是算错**;
> 产业上「无反硝化 RAS」只有 50–150,是因为补水强度高 3–10 倍。
> **在这套换水率下反硝化是必需项,不是可选项。** 详见技术手册 12.4。

**复审轮次的追加修复**

主体修复完成后又接受一轮复审验证(逐行 diff + 重建复算环境 + 逐条设计用例复跑),发现并修复:

| 级别 | 问题 | 说明 |
|---|---|---|
| 严重 | **热泵装机口径分裂** | 修计价时只改了 CAPEX,其余三处没跟上 —— 同一台热泵页面上出现 **4 个数、最大差 13.6 倍**(40 / 冬40夏321 / 175 / 545 kW)。自然冷却让问题更糟:冬季电耗掉一个数量级,设备清单的定容工况从冬悄悄切到夏,而 CAPEX 用热功率冬夏都没变 —— 两条链主导工况都不同了。已抽出统一函数,四处改用热功率 ÷ 22h |
| 严重 | **零流量水质链 NaN** | 空场防护只清了成本链,沿程水质节点仍 NaN。而面板格式化是 `typeof n === 'number' ? n.toFixed(d) : n` —— **NaN 也是 number**,界面上最显眼那块直接显示「NaN mg/L」 |
| 严重 | **防除零下限 0.001**(v1.0 老 bug) | 它实际是「单次循环去除率下限 0.1%」,而典型 RAS 单次稀释只有 0.02–0.08%,**正常工况普遍低于它**。后果:换水率 ≤ 4.8%/d 时 NO₃ 被硬钳成同一个值,1%/2%/4% 三档都输出 339.9(理论 1657/829/414) |
| 重要 | **硝化通量与 η 完全解耦** | η=0 时模型一边报告 TAN 发散、一边照常按全量产 NO₃ 并耗全额碱度。改为「硝化量 = 产生量 − 换水排出量」后,η=0 时 NO₃ 与 NaHCO₃ 同步归零 |
| 一般 | NH₃ 阈值写死 25 μg/L | 改为按鱼种(鲑科 12.5 / 罗非 100)。原值对冷水鱼偏松一倍、温水鱼偏严 2–4 倍 |
| 一般 | 方解石饱和指数 Ω 无告警 | 新增欠饱和告警。实测四个海水鱼种 Ω 0.55–1.53（v2.5 链式重导后）——多数阶段欠饱和，对虾苗种期过饱和 —— 长期溶蚀混凝土池体、不利甲壳类蜕壳 |
| 一般 | 自然冷却无 UI 露出 / 参数不可调 | 加 ❄️ 标记;三参数接入围护区,全链路贯通 |
| 一般 | 标题成本未标工况 | 自然冷却使**设计冬变成全年最省**(35.59 / 年均约 36.9 / 设计夏 37.37),不标注读者易当年均值。已加 `[设计冬工况]` 标签 |

**自检增补不变量断言** — 复审指出 golden-master 基线只能锁行为、不能发现系统性错误(8 项缺陷里只有 1 项能被抓到)。已补两类不依赖「当前值是对的」的断言:**口径一致**(热泵装机三处必须同值)与**边界有限性**(遍历所有返回对象的每个数值字段,不得 NaN/Inf),外加三个边界工况。这套断言加上当场就抓出了零流量的 5 个 NaN 字段。

> **一个值得记的连锁**:发散检测最初把阈值设为 `R ≥ 0.999`,那些被 0.001 钳制的工况全被标成「无稳态解」——
> 表面上是模型诚实告警,**实际是用一个错误的告警盖住了一个错误的数**。阈值改到 `1 − 1e-9` 后告警消失,
> 数值错误才暴露。**过于宽松的告警阈值比没有告警更危险。**

**新增:自检入口** — 见下方「自检」章节。

**技术手册新增侧边导航** — 366 条目录链接（v1.9.3 补入 7 条），支持滚动跟随高亮、章节过滤、快捷键（`M` 开关 / `/` 聚焦 / `Esc` 关闭），打印时自动隐藏。
> ⚠ 目录为**硬编码**而非运行时自动生成 —— 新增章节必须同步补入导航，否则新内容从侧边栏点不到（v1.9.3 审计正是发现了这个脱节）。

### v1.9.3:基线对齐 + 无头测试套件

**不改动任何计算模型** —— `?selftest=1` 的 14 项基线逐位不变，设备选型亦逐位不变
（mainline/bypass 对照的主泵 369.0 / 258.3 kW、旁路泵 92.2 kW 与旧表一致）。

**四工况基线对齐**

v1.9.2 发布的四工况表无法用产品路径复现，三种气候口径全部对不上：

| 口径 | 青岛冬 | 海南冬 | 海南夏 | Bergen |
|---|---|---|---|---|
| ① 冬夏都取该行温度（自检所用） | 35.59 | 36.95 | 37.37 | 35.61 |
| ② 只覆盖冬、夏留 UI 默认 | 35.74 | 37.00 | 37.37 | 35.75 |
| ③ 只覆盖冬、不动 RH | 35.74 | 36.99 | 37.33 | 35.75 |
| **v1.9.2 已发布** | **35.63** | **37.39** | **37.94** | **35.65** |

判定为成本模型两层化之前的旧脚本残留，现按口径 ① 重测替换。
**这与 v1.9.2 审计发现的 32.65 元/kg 问题同源** —— 根因都是「验证路径与产品路径不是同一条」，
只是这次残留在文档侧。

**无头测试套件**

`npm test` 一条命令跑**十三项**，任一失败非零退出，可直接挂 CI：

| 脚本 | 覆盖 | 判定 |
|---|---|---|
| `t1_selftest.js` | 内置 `?selftest=1` | 14 工况全 PASS + 两类不变量断言 |
| `t2_roundtrip.js` | 存/取方案 | 改 65 个控件后 summary 全字段零漂移 |
| `t3_downstream.js` | 下游页面数据链路 | 载荷与渲染文本均无 NaN |

另有 `gen_baseline.js` 由产品路径生成文档表格 —— **基线表今后应粘贴它的输出，不要手抄**。

### v1.9.3（P1+P2）：部分自然冷却 + 年平均年化

**主要矛盾不是「逼近温差取几 K」，而是判据「非 0 即 1」。** 旧判据一旦跨过阈值，
EER 从 3.45 跳到 20、Q 从 0 跳到全量，两个量同时跳变 —— 阈值附近出现 **5.8 倍**成本悬崖。

**P1 容量判据**

```
Q_fc      = min(K_fc · ΔT_avail, Q_load)     K_fc = Q_design / ΔT_design
EER_fc    = EER_design · min(ΔT_avail/ΔT_design, 1)
1/COP_eff = (1−g)/COP_cool + g/EER_design
```

`Q ∝ ΔT` 是**精确**的而非线性化（ε-NTU：流量固定时 ε 为常数）。
**经济截止点自动涌现且恰好连续**：截止处 `EER_fc = COP_cool`，两条路径边际成本相等，
故 E 两侧完全相等 —— **悬崖不是被磨平，是不存在了**，且无需为它引入任何参数。

参数 `freeCoolingApproach_K` → `fcDesignDT_K`，语义由「悬崖位置」变为「单独扛满设计负荷所需的温差」，
是**换热链串联损失**：空气源（板换 2–3 + 干冷器对空气 5–10）= 8–13 K；水/开式海水（钛板换）= 3–6 K；地源 5–7 K。
UI 留空即按热源自动取（空气 11 / 水·海水 4 / 地源 6）。**填错几 K 只会让曲线挪一小段。**

**P2 年平均：24 点数值积分**

阈值函数在均值处取值 ≠ 函数的均值（Jensen）—— 旧模型切设计冬判定全年可用（高估）、
切年均判定全年不可用（低估），**真值在中间而单点永远给不出**。
用现有三个温度点拟合正弦，T_air 与 T_source 各自振幅、同相位摆动，逐点跑真实热平衡再平均。

闭式解保留为诊断量：**对贡献率精确**（青岛 0.3826 vs 收敛数值 0.3826），
但**能耗偏乐观 1–7%**（未计 Q_load 与冷源温度的相关性）—— 这正是改用数值积分的原因。
点数取 24：N=12 误差 −1.24%~+1.03%，N=24 降到 0.15% 以内；成本仅 0.9 ms。

| 地区 | 热源 | 设计冬 | 年均 | 设计夏 | 年化贡献率 | 年化 COP |
|---|---|---:|---:|---:|---:|---:|
| 挪威 Tromsø | 开式海水 | 37.62 | **37.64** | 37.67 | 98% | 18.14 |
| 挪威 Bergen | 开式海水 | 37.66 | **37.95** | 39.07 | 73% | 7.82 |
| 智利 Puerto Montt | 开式海水 | 37.67 | **38.13** | 39.07 | 59% | 5.95 |
| 加拿大 Halifax | 开式海水 | 37.69 | **38.20** | 39.42 | 65% | 5.85 |
| 青岛 | 空气源 | 37.75 | **38.54** | 39.35 | 35% | 4.64 |
| 上海 | 空气源 | 37.80 | **38.81** | 39.54 | 19% | 3.93 |
| 三亚 | 开式海水 | 39.62 | **39.98** | 40.42 | 0% | 2.30 |
| Doha | 水源 | 39.24 | **39.68** | 40.26 | 0% | 2.68 |

> **一个反直觉的反转**：直觉上青岛 −7°C 的冬天该是自然冷却之王，但**年度账上稳定的 9°C 海水赢两倍以上**
> （Bergen 73% vs 青岛 35%）—— 因为海水**全年都冷**，而青岛的空气只冷一段时间。
> 旧模型只会说两地设计冬都是 COP 20.00。**年化改变的是选址与冷源方案的排序，不只是把数字调准。**

**已知缺口（未修）**

1. ~~振幅折减 k 待标定~~ → **已核验，无需改动**。敏感性实测表明，ḡ 的主导不确定性**只有季节振幅 A 一个量**；
   而 A 已对多个独立气候常值来源核验：青岛空气 模型 12.7 vs 实测 12.1–13.1；Bergen 海水 4.5 vs 4.0–5.25；
   Halifax 海水 8.5 vs 8.2–8.9。**三处全部落在实测区间内。** 其余 5 个区域仅作合理性判断，尚未独立核验。
   校准已由 `t7_amplitude_calibration.js` 锁死。
2. ~~昼夜与天气过程未计~~ → **降级为二阶**。实测: A 由实测月均钉死后，ḡ 对短期噪声 σ（1→6 K）的
   剩余敏感性仅 **0.1–2.2 pp**（A 未钉死时为 8.4–17.7 pp）。**忽略昼夜变化是有依据的，不是将就。**
3. ~~RH 按年均值恒定~~ → **实测为三阶，不予实现**。RH 的季节数据本已在各区域预设里
   （振幅 −2.5~+9.0 pp，两种相位都有：青岛/上海/三亚 夏湿同相，Bergen/Tromsø/智利 夏干反相），
   24 点积分只是没用它。按预设振幅摆动 RH 后，年化热泵电耗变化仅 **−0.07%~+0.30%**；
   即使振幅**翻倍**也只有 −0.15%~+0.60%。原因：RH 只作用于蒸发/潜热通路，
   而净排热由恒定内热主导（泵+代谢约 25948 kWh/d）。0.3% 远低于本模型精度等级。

**P1 核心主张已验证：参数杠杆确实被压平**

| 参数 | P1 之前 | P1 之后（全合理区间） |
|---|---|---|
| ΔT_design，Bergen 海水 | 6→10 K 即 **+1.20** 元/kg | 2→6 K 极差 **0.35** |
| ΔT_design，青岛 空气 | — | 6→16 K 极差 **0.20** |
| **ΔT_design，设计冬工况** | 悬崖 | 极差 **0.0000**（完全不敏感） |
| EER_design 12→30（2.5×） | — | 极差 0.11~0.22 |

单位 K 杠杆由 0.30 元/kg 降至 0.087（Bergen）/ 0.02（青岛），**约降一个数量级**；
敏感性全部落在年均工况——正是过渡带该在的地方。设计冬极差为 0 也正是 14 项基线中性的根据。

反悬崖指标：扫描中最大单步跳变仅占总极差 **30~31%**（阈值判据会接近 100%）。
已由 `t8_leverage_bounds.js` 锁死——谁把阈值判据改回去，它会立刻失败。
4. 自然冷却冷源与热泵冷源仍绑死；蒸发冷应走湿球而非干球 —— 属 P3。
5. 热侧用目标水温：实测 ΔT_loop 仅 **0.09 K**，此项**无需再改**。

> **✅ 自检 14 项基线逐位不变**：四工况全是空气源且落在饱和区。
> 最紧的是 Bergen 设计冬苗种期（x = 12/11 = 1.09），**余量仅 9%** ——
> 将来上调空气源设计温差会让 `salmon_bergen_w` FAIL，那是**预期行为**，应复核而非改基线。

> **头条口径**：原先以「k 未标定」为由建议暂不切换到年均；该理由**现已消除**（见上第 1 条）。
> 切换在技术上已站得住，但它会改动每一个对外数字与 `SELFTEST_BASELINE` 全部 14 项，
> 属**发布级变更**，应作为独立一步执行并同步重算全部基线，不宜挂在本版上。
> 当前年均作为**推荐的经济性视角**并列展示，计算默认仍为设计冬。

**比电耗与热泵电费：补出口，纳入自检**

这两个是对外发布的核心指标，但 `summary` 里**此前没有对应字段**（页面上的 `kwhPerKgYr` 只是热泵单项且分阶段），
因此进不了 `SELFTEST_BASELINE`、**无法自检**，至今只是「碰巧还对着」。
本版补上 `kwhPerKg` / `elecCostPerKg` / `hpCostPerKg` / `pumpShareOfElec` / `loxTonsPerYear`，
并把前两者写入全部 11 条数值基线。

| 指标 | 旧发布值 | 产品出口实测 | 差 |
|---|---|---|---|
| 比电耗（青岛冬） | 9.7 | **9.51** | −2% |
| 热泵电费（青岛冬） | 0.29 | **0.21** | **−28%** |
| 液氧年耗 | 473 t | 471 t | 核对无误 |

根因：旧手推口径对**臭氧与 BF 风机取了峰值**，而年成本按 `totalAvg × 365` 计算——
产品路径的均值口径才与成本模型自洽。四工况表的比电耗列已更新为 9.5 / 11.9 / 12.6 / 9.5，泵占电 57–75%。

**为什么不在汇总层手列 kWh 清单**：① **增氧可能是电**（`aerationMode==='air'` 时是主风机电费，
纯氧时才是液氧）——旧手推完全漏了这一项，只因大西洋鲑走纯氧才碰巧对上；② 臭氧、BF 风机、增氧都有峰/均之分。
故电量在**成本函数内部、紧挨 `totalAvg` 那一行**计算，复用它已用的成本项再除以电价，
可与 `totalAvg` 逐项肉眼对照。`gen_baseline.js` 里那份重复手推实现也已删除。

> ⚠ v1.9.2 变更日志里「热泵费 1.68 → 0.29」的叙述**保留原样**：那是历史记述，
> 而 1.68 那一侧的旧代码已不存在、无法测量，改成 0.21 会让叙述一半真一半编。当前值以基线为准。

**备用设备计入设备投资**

模型一直按 **N+1** 给每个模块配备用，设备清单也展开了备用台数，但 CAPEX 是**按工艺容量**计价的——
**备机只选型、不花钱**。实证：把「套数」由 1 改到 5，备用台数随之变化而设备直购恒为 4360 万。

**冗余系数 = 实际装机 / 工艺需求**。不能用 `(N+备)/N`——它对 N+1 正确，但对**超配并联失效**：
UV 是 2×75% 并联（装机 = 需求 ×1.5）而备用数为 0，会被算成 ×1.00，
即实际装 150% 只按 100% 计价。这是 v1.3 就存在、因从未按装机量计价而未暴露的漏算，本版一并修正（+35 万）。

**臭氧改 2×50% 并联**：原为 1+1 且 N 恒为 1（100% 冗余），成鱼期单台 286 万、备机等额，
单项占备用增量 48%。臭氧停机不像主泵那样 15–30 min 致鱼窒息，改为 2×50% 后装机 = 需求，
不额外增加投资，任一台故障仍维持 50% 氧化能力。

| 阶段 | 备用增量 | 主泵/RDF/热泵/臭氧 的 N |
|---|---|---|
| 苗种期 | 33.4 万 | 1 / 1 / 1 / 2 |
| 幼鱼期 | 121.3 万 | 1 / 2 / 1 / 2 |
| 中鱼期 | 182.5 万 | 2 / 3 / 2 / 2 |
| 成鱼期 | 288.8 万 | 2 / 4 / 3 / 2 |
| **全场** | **626 万** | N 随规模增长 → 冗余由 ×2.00 降至 ×1.25 |

备机与主机同乘 ×2.2 工程放大系数（**安装备用**）。结果：设备直购 4360 → 4986 万，
折旧 8.07 → 9.14 元/kg，**工艺运行成本 35.74 → 37.33 元/kg**。

同时新增**加药计量泵** CAPEX 条目（5000 元/台，此前有备用规格却无计价条目）、
补 **CO₂ 脱气塔风机**模块化规格。

> ⚠ 已知未含备用：**机械除湿机**属全场设备、不在模块环路内。本版仅确认其不配模块内备机，
> 未移入站级——实测其分区选型**不构成重复计算**，且因除湿需求只在夏季出现，
> `Σmax(冬,夏)` 恒等于 `max(Σ冬,Σ夏)`，移动不改变任何数字。

**第三轮外部审计修复（P0 / P1 / P2）**

- **P0 渲染层口径分歧**：工况对比表用 `heatPumpKWh / 16`，而正上方卡片用 `热功率 ÷ 22h`——
  **同屏两个数差 14 倍**（38 vs 548 kW）。数据层的不变量断言抓不到，因为它是渲染层局部变量。
- **P0 防除零 clamp 推翻经济截止**：`Math.min(0.999, COP_cool/EER)` 在 EER < COP_cool 时
  把「永不经济」硬拉成「几乎总经济」。实测 **EER=1 电耗 6277 vs 关闭自然冷却 1819——多耗 3.45 倍**。
  改为 `x ≥ 1 直接置 g = 0`；EER 钳到 [1, 60]；界面新增「自然冷却不经济」提示。
- **P1 热泵漏出冗余映射**：设备清单与 P&ID 按 N+1 展开，唯独 CAPEX 按 N 计价。备用增量 626 → **725 万**。
- **P1 年均基线缺失**：11 条工况全跑设计冬，4 条大西洋鲑又用冬夏同温的隔离口径 ⇒ 年振幅为 0，
  **24 点积分在内置自检里一次都没被执行**。已新增年均工况，自检 14 → **15 项**。
- **P2**：氧锥旁路泵补进支路泵冗余数组（bypass 拓扑下占该项 46%）。

**数值钳制（clamp）普查**

本项目两次栽在 `Math.max/min(…, 常数)` 上，**两次都是外部发现的**——
这类错误不产生 NaN、不越界、不让基线 FAIL，现有三类断言结构上抓不到。

引入**扰动测试**：把常数改到「不可能生效」的值，比对全场输出指纹；变了即说明它正在生效。
**分组二分**使 126 项候选只用 38 次 jsdom（逐个跑需 128 次）。

| 类别 | 候选 | 正在生效 |
|---|---:|---:|
| C! 极小常数 | 20 | **0** |
| C 待判定 | 60 | 2 |
| B 物理边界 | 38 | 2 |

**4 处生效项逐条判定后全部为有意设计**，没有第三个 `0.001` 那类错误。
最令人担心的 COP 下限 1.5/1.8/2.0、β 区间、RH 域，在正常工况下**全部不触发**。
全表见 `CLAMP_INVENTORY.md`；哨兵 `t11` 秒级检出「新增了没人测过的 clamp」。

> ⚠ 覆盖边界：指纹只覆盖三文鱼+罗非鱼、设计冬+年均，**只在极端参数下越界的 clamp 会漏**；A 类 65 项未测。

**热泵装机：收敛到单一出口**

审计建议「让装机 kW 只有一个出口」。普查后发现消费点**不是 4 个而是 5 个**——
第五个是 `summary.totalHeatPumpCapacityKw`，用**电耗 ÷ 小时数**而非**热功率 ÷ 小时数**，
实测**青岛 36.6 vs 997.3 kW（差 27 倍）**，且它进导出载荷与下游页面。
另发现利用小时的解析逻辑**被复制了 4 份**。已抽出 `hpUtilHoursOf()` 唯一出口，
并新增 `t12` 三层断言，其中一层**直接读渲染出的 DOM 文本**——这一层正是原断言的盲区。

**水质告警：NH₃ 按鱼种 + Ω 方解石**

- **NH₃**：鱼种限值表早已存在（鲑 12.5 / 罗非 100 …），tooltip 也知道，**但判据写死 25 μg/L**——
  最敏感的鲑科用了 2× 宽松阈值。改为三档（超限值红 / 超半限值黄 / 其余绿）。
- **Ω 方解石饱和度**：此前**界面上完全没有显示**（已算出并进导出载荷，只是不渲染）。
  实测**海水鱼种多数阶段欠饱和**：三文鱼 0.55–0.72、大菱鲆 0.56–0.75、石斑 0.63–0.81、对虾 0.90–1.53（对虾苗种期 1.53 为过饱和，其余全部 < 1）。
> ⚠ 本组数值随 v2.5 链式重导（审计 B8）更新过一次；旧值 0.37–1.29 出自集总口径下偏高的鱼池 CO₂。现由自检断言 N2 锚定。
  这是模型算出的真实状态——高密度 RAS 中 CO₂ 累积压低 pH。
  **Ω < 1 会持续溶蚀混凝土池体与水泥基砂浆。**

> ⚠ **蒸发计入补水（P3 第三项）经实测后决定不做**：蒸发仅占补水 **0.06%–0.32%**，
> 折算成本 < 0.001 元/kg，而改动要把 12 条基线与三份文档数字再重算一遍。
> 该结论依赖默认 `poolCover=partial` 且换水率 5%/d；敞开池面 + 干热气候时占比会显著上升。

**上述盲区已修**

比电耗与热泵电费**没有产品出口**，因此进不了 `SELFTEST_BASELINE`、无法自检，现在只是碰巧还对着。
建议下一版补 `kwhPerKg` / `hpCostPerKg` 两个字段并纳入基线。

> **年化基准的坑**：全场年耗电与年液氧按**日值 × 365** 累加（各阶段全年并行运行）。
> 按 `stageDays` 串行累加会把比电耗低估 2.3 倍（4.2 vs 9.7）。已用年液氧 471 t 作独立锚点反查确认。

### 资料库:RAS 工程手册合集 📚

主工具之外,新增 **iRAS 资料库** — 一组原创 RAS 工程深度手册,每项数据均标注文献或法规出处。主页顶部导航新增"📚 资料库"入口。

- **鱼池水力学与流场控制工程手册 (v0.1)** — 圆池 / 八角池 / 方圆角池水力学对比、流速生理准则 (BL/s)、切向进水设计、深径比、CFD 工程应用与现场调试。含决策树、文献值与设计算例 (8 章 + 4 附录 + 40 表)
- **RAS 标准操作规程框架与水质参数手册 (V1.0)** — 启动 / 日常运行 / 应急处置三大场景 SOP 框架,配大西洋鲑 Post-smolt / 成鱼阶段水质运行参数限值表,每项数据标注文献或法规出处,可直接用于本场 SOP 制定
- **iRAS 技术手册 (v1.9.2)** — 16 章完整工程设计方法论,在线 HTML 阅读版。v1.9.2 新增:池内耗氧 TSS 依赖、代谢产热重构、气泡病 TGP 约束、成本两层口径、Nordic Aqua 宁波象山产业对标
- **统一阅读体验** — 全部支持在线阅读 + 浏览器"打印 / 存为 PDF",无需单独维护 PDF 文件

🌐 资料库入口:[https://iras.cn/library.html](https://iras.cn/library.html)

---

## 9 工况实测数据 (v1.9.3 实测)

电 0.7 元/kWh、饲料 12 元/kg、液氧 1.2 元/kg。

> **⚠ 本表含两种气候口径，取自 `SELFTEST_BASELINE` 原样，不可混读：**
> - **salmon 两行** = 青岛冬**隔离口径**（设计冬/设计夏均取 −7°C/4°C，用于隔离气候单一变量）；
> - **其余 7 个鱼种** = **UI 默认气候**（设计冬 −7/4、设计夏 32/24），即用户打开页面不改气候参数时看到的数。
> 同一鱼种两种口径相差 0.1–0.5 元/kg，引用时务必注明是哪一种。

> **v1.9.2 自然冷却生效**:青岛冬工况下室外 -7°C，远低于目标水温 15°C，
> 可直接用干冷器/板换排热而**不启动压缩机**（能效比 20 vs 机械制冷 3.45），
> 热泵费从 1.68 降至 **0.29 元/kg**。温暖气候（海南冬 17°C）达不到判定条件，仍走机械制冷。
> 这使跨气候成本差从 0.99 拉大到 **1.78 元/kg** —— 寒冷气候本就该因免费制冷而占优，
> v1.9.2 之前的模型把这个优势完全抹掉了。

| 鱼种 | 产量 | 盐度 | 工艺运行成本 | 企业全成本 | pH | NH3-N (μg/L) | Ω 方解石 |
|------|------|------|------------|-----------|------|--------------|---------|
| salmon (三文鱼) mainline | 1000t | 30‰ | **37.53** | **42.52** | 7.24 | 1.8 | 0.68 |
| salmon (三文鱼) bypass | 1000t | 30‰ | **35.94** | **40.87** | 7.24 | 1.8 | 0.68 |
| turbot (大菱鲆) | 500t | 28‰ | 50.75 | 60.64 | 7.10 | 2.1 | 0.50 |
| tilapia (罗非鱼) | 1000t | 淡水 | **29.08** | **33.63** | 7.47 | 7.7 | 淡水 |
| grouper (石斑鱼) | 500t | 30‰ | 41.40 | 50.91 | 7.04 | 4.0 | 0.64 |
| bass (加州鲈) | 300t | 淡水 | 43.80 | 50.62 | 7.46 | 9.0 | 淡水 |
| eel (鳗鲡) | 200t | 淡水 | 51.07 | **106.52 ⚠️** | 7.60 | **12.3 ⚠️** | 淡水 |
| mandarin (鳜鱼) | 200t | 淡水 | 54.97 | 66.61 | 7.57 | 11.5 | 淡水 |
| shrimp (对虾) | 100t | 15‰ | 46.66 | 63.54 | 7.23 | 4.6 | 0.89 |

**两个口径的区别**:工艺运行成本含饲料 + 工艺电耗 + 工艺消耗品 + 维护 + 保险 + 补水 + 排污 + 折旧;
企业全成本再加苗种 + 人工 + 管理费。两者**均不含**包装/物流/加工、疫苗药物、顾问技术服务费。

**⚠ 鳗鲡参数已按国际 RAS 产业数据整体重做 (v1.9.2)**

| 参数 | 旧值 | 新值 | 依据 |
|---|---|---|---|
| 养成期密度 | 40 kg/m³ | **120** | 欧洲 RAS「120 kg/m³ 不罕见」，50 g 入池后可达 100–150 |
| 上市规格 | 200 g (5P) | **300 g (3P)** | 3P 美洲鳗批发 92 元/kg，美洲鳗为国内主养品种 |
| 换水率 (养成) | 4%/d | **6%** | 国际 RAS 5–8%/日 |
| 周期 | 12 月 | **15 月** | 单尾生长期 10–14 月至商品规格 |
| 苗种单价 | — | **10 元/尾** | 美洲鳗；缅因州玻璃鳗配额独立反推 10.8 元/尾 |

密度是主因：旧值低 3 倍 → 水体大 3 倍 → 系统与折旧高 3 倍。修正后工艺运行成本由 67.4 降至 **48.42 元/kg**。

**苗种成本 44.4 元/kg**（4.44 尾/kg × 10 元），占企业全成本 43%，接近行业经验的 50%。
鳗苗**全部依赖野生捕捞、无人工繁育**，品种价差极大：日本鳗 15–25 元/尾（歉收年 40+）、
美洲鳗 5–14、花鳗/鲈鳗 3.8–5.8。**做鳗鲡项目时苗种单价是第一敏感因子**，必须按品种与当年行情覆盖。

> **⚠ 关于成本验证**：鳗鲡的**工艺参数**（密度/规格/换水/周期/苗价）有国际 RAS 产业数据支撑；
> 但**成本绝对值无可用外部对标** —— 国内上市公司出池量中自用转加工占比高（2026 Q1 为 86%），
> 营收÷产量反推失真；欧洲 Nijvis、ALBE 等均为私营不披露；FAO 各国成本数据过时约二十年，
> 且丹麦鳗鱼产量已由 2020 年萎缩至 2024 年的 101 吨（年均降 37%）。
> 方向性旁证：本模型算出「养美洲鳗略亏」（103.73 vs 3P 批发价 92），与 2025 年行业低谷、
> 龙头企业全年亏损 1.79 亿元同号。

**⚠ NH3-N 告警** — 鳗鲡 12.3 μg/L 接近安全上限,因其 tempOpt 28°C + 高 pH 使非离子氨占比升高。

---

## 快速开始

### 方式 1:在线使用 (推荐)

直接打开,无需安装:

| | 地址 |
|---|---|
| 主站 | **<https://iras.cn>** |
| 国内服务器 | **<http://iras.dengdengtech.cn/>** |

国内网络访问 `iras.dengdengtech.cn` 通常更快。

### 方式 2:下载离线版

从 Releases 页面下载最新版 zip 包,解压后双击 `index.html` 即可在浏览器中打开。

- 🌍 **GitHub Releases**:<https://github.com/chengsunmail/iras/releases>

### 方式 3:本地克隆

```bash
git clone https://github.com/chengsunmail/iras.git
cd iras
# 双击 index.html 在浏览器中打开
```

### 系统要求

- 任何现代浏览器(Chrome / Edge / Firefox / Safari)
- **无需安装,无需后端,无需联网**(首次加载 Tailwind / nunjucks / docx.js / SheetJS 需要联网,之后可离线使用)

---

## 文档

📚 **iRAS 资料库**:[https://iras.cn/library.html](https://iras.cn/library.html) — 原创 RAS 工程深度手册合集

收录手册:
- 💧 **鱼池水力学与流场控制工程手册** — 池型水力学、流速准则、切向进水设计、CFD、现场调试 (8 章 + 4 附录 + 40 表)
- 🧪 **RAS 标准操作规程框架与水质参数手册** — 启动 / 运行 / 应急 SOP + 大西洋鲑水质参数限值表 (每项标注文献或法规出处)
- 📖 **iRAS 技术手册 v1.9** — 16 章完整工程设计方法论 (在线阅读 + 打印存 PDF)

📖 **技术手册主要章节**:
- 第 1-3 章:RAS 工艺原理 + 水质参数与控制标准
- 第 4 章:氮碳循环 + **§4.5 碳酸盐平衡 (v1.9 新)**
- 第 5-7 章:养殖规模设计 + 污染负荷计算 + 精确循环方程
- 第 8-15 章:RDF / BF / UV / 蛋分 / AOP / 反硝化 / CO₂ / 增氧设备设计
- 第 16-22 章:CAPEX / OPEX / 财务评价方法
- 第 23-25 章:三文鱼 / 大菱鲆 / 罗非鱼工程案例 (含 v1.9 碳酸数据)
- 附录 A-F:完整符号表 + 默认参数表 + 鱼种参数表 + **参考文献 40+ 篇** (含 Millero 2010, Clegg & Whitfield 1995, Dickson 1990 等碳酸文献)
- **v1.7 → v1.9 演进章节**:碳酸求解 + 物理校准 + CO₂ 修正 + finance 修复完整说明

📝 **在线使用说明**:在主页面点击右上角 "? 帮助" 按钮

📋 **v1.9 Release Notes**:[完整版](v1_9_RELEASE_NOTES.md)

---

## 自检 (Self-test) 🔧 面向二次开发者

**普通用户日常使用无需理会本节。** 直接打开 `index.html` 填参数即可。

唯一可能用到的场合是**怀疑软件算错了**：访问 `index.html?selftest=1`，
它会跑 14 个固定工况与内置基线比对并弹出结果面板。

- **全 PASS** → 计算内核完好，问题在输入参数或对结果的理解
- **有 FAIL** → 这份文件被改动过或已损坏，别用它出报告

改过模型或做二次开发的人，请改完立刻跑一次；命令行版本：

```bash
npm install && npm test        # 九项，任一失败非零退出
```

完整说明（受检字段、不变量断言、容差、突变测试验收办法）见 [`tests/README.md`](tests/README.md)
与技术手册「版本演进说明 → v1.9.2 → v1.9.3 → C 节」。

**为什么需要它**：v1.9.2 审计发现，此前对外发布的四工况基线是用一份**外部测试脚本**跑出来的，
而该脚本写于成本模型扩展之前，调 `calcCostEstimate` 时少传三个参数——它算的是旧口径，
误差 3.41 元/kg，被写进三个文件对外发布。根因不是某个公式错，
而是**验证路径与产品路径不是同一条**。因此本自检的唯一设计约束是：
必须调用真实的 `recomputeAll()`、读真实的 `lastResults.summary`，不得重新实现任何计算管线。

## 使用场景

iRAS 适用于以下场景:

- ✅ **设计院**:RAS 项目的早期工艺方案 / 概算 / 可研草稿
- ✅ **业主 / 投资方**:RAS 项目可行性快速评估
- ✅ **设备商 / 总包**:项目选型与方案对比
- ✅ **高校 / 研究者**:RAS 工程教学与课程设计 (v1.9 碳酸模块对接 PyCO2SYS 实验室标准)
- ✅ **政府机构**:RAS 产业园规划与项目评审

iRAS **不适合**的场景:

- ❌ 替代正式的环境影响评价 / 安全评价 / 节能评价(必须由有资质机构出具)
- ❌ 替代施工图详细设计(本工具是工程概算阶段,精度 ±40%)
- ❌ 替代专业财务软件做 Monte Carlo 模拟(本工具是单点确定性现金流模型)

---

## 引用

如在学术论文、工程报告或商业项目中使用 iRAS,请引用:

```
孙程 (2026). iRAS 循环水养殖工程设计平台 (Version 1.9.0).
海南登登科技咨询有限公司. https://iras.cn
```

或 BibTeX 格式:

```bibtex
@software{iras2026,
  author       = {Sun, Cheng},
  title        = {iRAS: A Platform for Recirculating Aquaculture
                  System Engineering Design},
  version      = {1.9.0},
  year         = {2026},
  publisher    = {Hainan Dengdeng Technology Consulting Co., Ltd.},
  url          = {https://iras.cn}
}
```

---

## 协议与商业服务

本项目以 **GNU Affero General Public License v3 (AGPL v3)** 协议开源。

**重要提示**:AGPL v3 要求,如果您**通过网络向用户提供 iRAS 的服务或基于 iRAS 修改的服务**,您必须以相同协议开源服务端代码。详见 [LICENSE](LICENSE) 文件。

如需以下服务:

- 📋 **可研报告 / 工艺方案咨询**
- 🔧 **iRAS 二次开发或定制**
- 📚 **行业培训或专题讲座**
- 🤝 **项目深度合作**

请通过邮件联系:**6881509@qq.com**

---

## 维护方

**海南登登科技咨询有限公司**
作者:**孙程**(水处理工艺工程师, 挪威 NTNU 博士)

> 由水处理工艺工程师与 AI 协作开发,
> 物理模型基于公开文献和工业实测数据校准:
> - **核心工程**:Timmons & Ebeling 2010, 国家海水鱼产业技术体系, Atlantic Sapphire / Nordic Aqua 公开数据, Linde SOLVOX, Pentair AES Speece, Sharrer 2010
> - **碳酸水化学** (v1.9 新增):Millero 2010, Dickson 1990a/b, Clegg & Whitfield 1995, Mucci 1983, PyCO2SYS 1.8.3

---

## 版本历史

| 版本 | 日期 | 主要内容 |
|---|---|---|
| **v2.1** | **2026-08-01** | **能量守恒修复** —— v2.0 删除空气节点时, `Q_air_gain_kW` (电机/灯管废热) 与 `dehumHeatDest='indoor'` 的冷凝热成为孤儿项, 既不在水侧也不在暖通侧, 带走 HVAC 显热负荷 41.8% (青岛设计冬 1,617/3,864 kWh/d); 修复后成本变化符号随制热/制冷方向翻转, 为正确性旁证。**守恒类断言落地** (审计建议三轮未落地): 设备产热分配 / 车间显热闭合 / 水侧净热闭合三条并入 `?selftest=1` 及外部 `t20`; 修复前断言 B 在 56 个组合中 48 项失配, 残差与 `Q_air_gain×24` 逐位相等。**自然冷却可见性回归**: `freeCoolingSinkT` 被单行编辑误并进注释致 UI 第四档永不触发, `fcUnavailReason` 无消费点, 且 `not_cool` 被困在制冷分支内 —— 三层一并修复并改读枚举。另: `totalMax` 补 `hvacCostDaily`; `dehumEnabled` 开关无效却 gate 显示; 尾气入室比例默认 0.2→0 (对齐 A5); `co2StripperGtoL` 钳到 [1,30]; 鱼种默认气候联动 (罗非比电耗 16.21→13.48)。13 条基线全部重算 |
| **v2.0.1** | **2026-08-01** | **补丁版, 计算模型逐位不变** —— 修复模块化聚合 `totalKw_field` 对流量定额设备 (RDF 走 `configureFlow`) 产出 NaN 的不对称守卫 (16 工况 × 全阶段仅此一条非有限路径, 经 JSON 静默变 null, 下游未消费, 属潜伏缺陷); 消除容积率 FAR 默认值 0.5 / 0.7 两个口径并存 (`collectState` fallback + 注释 + 用户可见文案共三处, 其中存盘/导出不一致可致总占地静默缩小 29%); 页脚与版本面板改为从 `IRAS_META` 注入 (单一事实源), 新增自检项 `meta_version_consistency` 锁定 SEO 串与注入结果, 已用负对照验证非恒真; 自检 16 → **17 项** |
| **v2.0.0** | **2026-07-31** | **热平衡架构重构** —— 车间温度由「自由漂浮的因变量」改为「暖通维持 = 水温」输入，删除双节点求解器（Picard 迭代 / 闭式解 / 钳位，净减 65 行）；**首次计入气液接触器**（CO₂ 脱气塔 + MBBR 曝气，96,287 m³/h，为机械新风 4.2 倍）的热湿交换，出口按水温饱和（Karimi 2020 + Talati & Stenstrom 1990 双实证）；**暖通显热计入能耗**（此前仅除湿机计入）；脱气塔默认 G:L 3→5、填料高度 1.5 m（Timmons Ch.10），比功率 3.47→5.79 kW/1000m³h 进入 Summerfelt 区间；水质限值按温水/冷水分档（Tab 3.1）；修正鳗鲡/罗非/大菱鲆三处参数组合的物理不可行（鳗鲡养成期原出池 DO = 0）；基线工况按鱼种×地区实际配对重设并新增热带三文鱼工况；自检 14 → **16 项**，新增 t15–t17 三类断言 |
| **v1.9.3** | **2026-07-30** | **基线对齐 (四工况旧数无法用产品路径复现, 按真实 recomputeAll() 重测) + 无头测试套件 (自检 / 存取 roundtrip / 下游链路, 可挂 CI) + 基线表改由 gen_baseline.js 生成;计算模型与设备选型逐位不变** |
| v1.9.2 | 2026-07-18 | **通风量口径修正 (新风按鱼载荷 CO₂ 需求, 消除"密度降低→制冷暴涨") + 机械除湿机建模;泵功率口径修正 (新增 motorEta, 车间得热原高估 2.8 倍) + 代谢产热改由耗氧经氧热当量导出 (原高估 3 倍) + 池内耗氧新增 TSS 依赖 (固体管理首次与液氧账单挂钩) + 气泡病 TGP 约束建模 + 纯氧饱和度统一 165%;成本模型两层化 (工艺运行成本 / 企业全成本, 新增维护/保险/补水/排污/苗种/人工/管理 7 项);审计修复 6 项;设备选型逻辑不变** |
| **v1.9.1** | **2026-07-18** | **液氧消耗口径修正 (成本高估 65% → 修正,基准值 0.44 → 0.266) + 蒸发系数 β 量纲修正 (Lewis 类比,修正 ASHRAE 错误出处);设备选型不变** |
| **v1.9** | **2026-06-28** | **碳酸盐平衡求解 (vs PyCO2SYS 验证) + O₂ Q10 物理校准 + CO₂ 公式修正 (加硝化产 CO₂) + finance 零利率 bug 修复;配套上线 iRAS 资料库 (水力学手册 + SOP/水质手册 + 技术手册在线版)** |
| v1.7 | 2026-05 | 氧锥拓扑 mainline/bypass + K-101 air 模式 + 系统排水重构 + 审计修复 27 项 |
| v1.6 | 2026-05 | 站级设备(液氧储罐)+ UI 重构 + 8 项工程逻辑精化 + 6 处手册公式订正 |
| v1.5 | 2026-05 | 可研报告生成器(16 章 + docx + 模板引擎) |
| v1.4 | 2026-05 | 投资财务评价(NPV/IRR + 8 张标准表 + Excel 导出) |
| v1.3 | 2026-05 | 多模块并联 + N+1 备用 + P&ID + 设备清单 |
| v1.2 | 2026-05 | 全局气候 + 双节点稳态热平衡 + 4 工况对比 |
| v1.1 | 2026-04 | 物理框架重构 + CAPEX/OPEX + V_total 完整建模 |
| v1.0 | 2026-04 | 稳态浓度计算 + 设备选型 |

> v1.8 不发布。中文版直接从 v1.7 跳到 v1.9, 与英文版同步。

---

## 免责声明

本工具计算结果仅供工程概算参考。

> **关于成本口径**:v1.9.2 起分两层输出 —— **工艺运行成本**(饲料 + 工艺电耗 + 工艺消耗品 + 维护 + 保险 + 补水 + 排污 + 折旧)与**企业全成本**(再加苗种 + 人工 + 管理费)。两者**均不含**包装/物流/加工、疫苗药物、顾问技术服务费,这三类随项目差异极大须另行估算。
>
> **关于工艺参数**:生物滤池动力学、各类去除率与安全系数为**通用工程取值**,用于方案比选与量级估算。这类参数随填料型号、运行浓度、菌群状态、设备厂家差异很大,不同来源实测值可相差数倍。**具体项目应以设备厂家实测数据或设计单位参数覆盖默认值,不应直接作为施工图依据。**

- 实际工程必须结合现场水质监测、批量试验数据、当地规范与专业工程师判断
- 投资概算精度约 ±40%,实际造价以厂商报价和工程招标为准
- 不替代正式的环境影响评价 / 安全评价 / 节能评价
- v1.7 氧锥拓扑 mainline / bypass 经济性对比基于 1000 t 大西洋三文鱼工况实测,中小项目(Q &lt; 5000 m³/h)bypass 优势减弱,建议项目具体核算
- v1.7 "系统排水"输出口径为**沉淀池入口**浓度(含污泥,质量守恒),不可直接对照 GB 8978 等污水管口标准
- v1.9 碳酸盐平衡输出 pH / NH₃ / Ω 为**日均稳态(慢性暴露)值**,不是投喂瞬时峰值。生产中投喂 2-4 小时窗口的瞬态峰值另需考虑(β 缓冲强度 + CO₂ 脱气塔容量限定其上限)
- v1.9 NH₃-N 限值为长期暴露(慢性)值,苗种期工具自动收紧到 50%。商业项目还需考虑短期峰值 (Calabrese 2017 / Fivelstad 2013 文献给出区间)
- 因使用本软件计算结果产生的工程决策后果,作者与公司不承担责任

完整免责声明见技术手册附录 F。

<!-- IRAS_README_V2_0 --><!-- IRAS_README_V2_ORDER -->

<!-- IRAS_DOC_FC_CONTACTOR_V2 -->
